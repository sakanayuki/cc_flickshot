# plan.md — コインゲーム「どうぶつの やまくだり」仕様書

このドキュメントが**唯一の仕様**。実装で迷ったらここを見る。

> **⚠️ 2026-08-01、3 つの前提のもとでゲーム画面を作り直した。**
>
> 1. **本書を正とし、旧「詳細設計書」は無視する。**
>    旧文書は [`docs/archive/detailed-design-v1.md`](docs/archive/detailed-design-v1.md)
>    に保管用として置いてある。仕様として読まないこと。
> 2. **物理は自作をやめ、Matter.js で駆動する。**
> 3. **年齢のターゲットを撤廃する。** 3歳児向けの特化をやめ、大人も楽しめるものにする。
>
> 経緯は §11 を参照。それ以前の改訂履歴もそのまま残してある
> (同じ袋小路に二度入らないため)。

---

## 1. コンセプト

- 昔の駄菓子屋にあった 10 円ゲーム機(エレメカ筐体)の Web 版。
  盤面には**ななめ上向きのレーン**が 5 段。重力は画面の下向きに働く。
  コインはレーンの低い端(画面の左右の端)で止まっていて、レバーで弾くと
  レーンを**登り**、重力で減速していく。
  レーンの途中から先は床が無く、**どこに落ちるか**で結果が決まる。

  ```
    弱すぎ  → 手前の穴に落ちる(アウト)
    ちょうど → 隙間に落ちる → 1 段下のレーンへ進む
    強すぎ  → 隙間を飛び越して奥の穴に落ちる(アウト)
  ```

  これを 5 回。最後の 1 回であたりの口に入ればクリア。

- **狙いは「同じ引き方を再現する」遊び。** 5 段すべて条件が完全に同じなので、
  1 回目で得た手応えをそのまま 5 回繰り返せるかどうかが勝負になる。
  年齢を問わず、短時間で腕前が上がるのが分かることを重視する。
- テーマは「**どうぶつの やまくだり**」。据置アーケード筐体の見た目に、
  どうぶつの意匠のメダルを 1 枚だけ落としていく。

### 実機との最重要な差分(発注者指定)

実機ではレバーは 1 本ずつ独立操作だが、本作では
**画面右下の「引っ張れるプランジャー」1 つで全レバーが連動して同時に動く**。
引っ張った量が大きいほど弾く力が強くなる。

---

## 2. 確定済み要件

| 論点 | 決定内容 |
|---|---|
| ゲームの向き | **上から下へ降りる**。適度な強さで弾けたときだけ隙間から落ちて 1 段下へ進む |
| 失敗条件 | **強すぎても弱すぎても穴に落ちてコイン没収**。両方向に失敗がある |
| クリア条件 | 最下段の隙間の先にある**あたりの口**に入る |
| 操作方式 | 右下プランジャー 1 つで全レバー連動。引き量 = 弾く力 |
| 物理 | **Matter.js**(§4.2)。判定用の特別ルールを持たず、剛体シミュレーションに決めさせる |
| 年齢ターゲット | **無し**。大人が遊んで手応えのある難度に取る |
| 画面構成 | タイトル / ゲーム / リザルト の 3 画面 |
| ゲーム開始 | 画面右上の投入口からのメダル投入アニメーション |
| リザルト遷移条件 | ①穴に落ちる ②あたりの口に入る ③あきらめる長押し の 3 つのみ |
| デバイス | **スマホ / タブレット縦向き優先**(PC では中央に縦長キャンバス) |
| 見た目 | **据置アーケード筐体**。暗色ベース、真鍮のベゼルとレール、ガラスの映り込み |
| サウンド | **音なし**(BGM・効果音とも実装しない) |
| 難易度 | **2 段階「やさしい / ほんき」**をタイトル画面で選択 |
| 盤面の大きさ | 画面の **74%**。操作部は **22%** |
| 段数 | **5 段**(+ 最後にあたりの口) |
| ごほうび | **メダル収集あり**。あたりごとにどうぶつメダル 1 枚、localStorage に永続化 |
| あきらめるボタン | 盤面の左上に小さく、0.8 秒長押しで発動(進捗リングを表示) |
| デプロイ | GitHub Actions で GitHub Pages に自動デプロイ |

---

## 3. 画面仕様

### 3.1 共通レイアウト

- 論理座標系: **720 × 1280(9:16 縦)**。全シーンこの座標系で描画する。
- 実画面へはアスペクト比維持でフィット(レターボックス)。余白は `COLORS.room` で塗る。
- `devicePixelRatio` を考慮して Canvas の実ピクセルサイズを設定する。
- **すべて Canvas 2D のコード描画**。外部画像・音声・フォント・CDN は一切使わない。

### 3.2 タイトル画面

- 筐体の看板(マーキー)、真鍮板に彫ったロゴ(光沢が走る)。
- ルールを 3 行で説明する。
- 難易度カード 2 枚。**カードにレーンの縮図を描き**、手前の穴と隙間の広さを
  そのまま図で見せる。どちらがどれだけ難しいかが数字なしで分かる。
- メダル棚: 10 種の枠を並べ、持っているものはメダルとして描く。2 周目以降は `×2` を添える。
- パワーメーターにショットの跡が残ることを 1 行で説明する。

### 3.3 ゲーム画面

```
+----------------------------------+
| [×]                     ▼投入口  |  ← 左上は長押しであきらめる
|  ┌─ ガラス窓(盤面) ──────────┐  |
| ▎│════════▁▁▁▁▁▁┈┈▁▁▁▁▁│  |  ← 段1。低い端は右。左上へ登る
| ▎│▁▁▁▁▁┈┈▁▁▁▁▁▁════════│  |  ← 段2。低い端は左。右上へ登る
| ▎│════════▁▁▁▁▁▁┈┈▁▁▁▁▁│  |     画面の 74%
| ▎│▁▁▁▁▁┈┈▁▁▁▁▁▁════════│  |
| ▎│════════▁▁▁▁▁▁┈┈▁▁▁▁▁│  |
| ▎│ ▕あたりの口▏                │  |
|  └──────────────────────────┘  |
| [POWER]   いま 2/5   [プランジャー] |  ← 操作部。画面の 22%
+----------------------------------+
   ▎= ベゼルに刻んだ段の目盛り(降りた段だけ光る)
   ══ = レール(床)   ▁▁ = アウトの穴   ┈┈ = 進める隙間
```

- レーンは**ななめ上向き**で、低い端(レバー)が段ごとに左右交互に入れ替わる。
  **レーンは画面の左右の端まで伸びている**(§4.1 の「死角を作らない」を参照)。
- レーン上の並びは 低い端 → **レール(床)** → 手前の穴 → 隙間 → 奥の穴 → 高い端。
- 開始時: メダル投入アニメーション(約 1.2 秒)。右上の投入口から入ったメダルが
  段 1 のレールに出て、斜面を滑り降りてレバーで止まる。
- 終了条件(いずれかで短い演出を挟んでリザルトへ):
  1. メダルが穴に落ちる(沈む演出 約 0.9 秒)
  2. メダルがあたりの口に入る(紙吹雪 約 1.4 秒)
  3. あきらめる 0.8 秒長押し

#### パワーメーター(本作の要)

操作部の左に縦のパワーメーター。ここには**直前までのショットの跡が残る**。

- 引き量の位置に、結果を色で刻む(**よわい** / **ちょうど** / **つよい**)。
- 直近 8 発ぶんを、新しいものほど濃く表示する。
- 直前のショットの判定は、操作部の中央にも文字で出す。

成功する範囲そのものは見せない。答えを配らずに「さっきより少し弱く」と
詰めていける道具だけを渡す、というのがこの表示の狙い。

### 3.4 リザルト画面

| 結果 | 見出し | 追加表示 |
|---|---|---|
| あたり | 「あたり!」 | 紙吹雪、メダル獲得の押印アニメ |
| 穴落ち | 「おしい」 | **弱すぎ / 強すぎ のどちらだったか**と、次にどう直すかの 1 行 |
| あきらめ | 「またね」 | — |

- 共通表示: 到達段数を 5 段のはしごで表示(降りた段だけ点灯)。
  最後に使った引き量を % で出す。次の狙いを立てる手がかりになる。
- ボタン: 「もういちど」(同じ難易度で再開)/「さいしょから」(タイトルへ)。
- **責める演出はしない。** 何が起きたかと、次にどうすればいいかだけを出す。

---

## 4. ゲームデザイン詳細

### 4.1 盤面構成 — 1 本のレールと、ひと続きの落とし口

レーン 1 本を低い端(u = 0)から見た並び:

```
  ├────────── レール(床あり) ──────────┤├─ 手前の穴 ─┤├─ 隙間 ─┤├─ 奥の穴 ─┤
  0                                    300          420       500        643
  ↑ レバー                                          ↑ここに落ちると 1 段下へ
```

- レーンは `ROW_COUNT` = 5 段。すべて合同(左右反転のみ)で、
  低い端が右・左・右…と交互に入れ替わる。
- **床はレール 1 本だけ**。`SOLID_RUN` から先は床が無く、ひと続きの落とし口になる。
  手前の穴・隙間・奥の穴は、その落とし口を u の範囲で 3 つに区切ったもの。
- 落ちた位置で結果が決まる。**弱すぎ → 手前の穴 / ちょうど → 隙間 / 強すぎ → 奥の穴。**
- 隙間から落ちたコインは 1 段下のレールに乗り、滑り降りてレバーで止まる。
- 最下段の隙間から落ちたコインは、そのまま**あたりの口**へ落ちる。

#### なぜ穴と穴のあいだにレールを挟まないか

挟むと、手前の穴を飛び越えたコインが**必ずその先の縁に着地する**。
縁は当たりどころで失う速度がまるで違う。実測では 943 px/s のコインが
縁に激突して 402 px/s まで落ちた。結果として、

> もう少し強く弾いたのに手前の穴に落ちた

という**飛び地**がパワーの範囲にできてしまい、
「弱すぎ → ちょうど → 強すぎ」が単調に並ばなくなる。

レールの端を離れたあと何にも触れないようにすると、落ちる位置は

```
  Δu = v・t − (1/2)・g sinθ・t²     t = sqrt(2・(COIN_R + PIT_DEPTH) / (g cosθ))
```

という単調な式になり、弾く力に対して 3 つの結果がきれいに並ぶ。
見た目は 3 つの落とし口をリブと色で区切って描き分ける。

#### レーンは画面の左右の端まで伸ばす

レーンの端と壁のあいだに余白を作ってはいけない。
隙間から落ちたコインは勢いを持ったまま高い端の側へ飛ぶので、
そこに空きがあると**その死角へ落ちて詰む**(弾くこともできず結果も出ない)。
端まで伸ばすと、壁が受け止めてそのまま 1 段下のレールへ落ちる。

> **弾いても何も起きない「空振り」を作ってはならない。**
> レールの端まで届かない弱さで弾くと、コインは滑り降りてレバーに戻るだけになる。
> `P_MIN` は「最弱でも必ず落とし口には届く」値より上に置く(検算が自動で落とす)。

### 4.2 物理 — Matter.js で駆動する

**物理エンジンは Matter.js を使う。** 自作の簡易物理は使わない。

- レール・あたりの口・盤面の壁は**静的剛体**。穴と隙間は**単に床が無い区間**。
  落ちるか渡りきるかを判定する特別なルールは持たず、剛体シミュレーションが決める。
- `src/game/` は Canvas も DOM も参照しない。Matter.js のコアも DOM 非依存なので、
  盤面をそのまま Node で回して検算・テストできる。
- 更新は**固定タイムステップ(60 Hz)**。1 フレームを `PHYS_SUBSTEPS` 回に分けて
  Matter に渡し、高速のコインがレールの角をすり抜けないようにする。

コインは常に次の 5 状態のいずれか:

1. **rolling** — レールの上、または落下中。物理に任せる。
2. **ready** — レバー(壁)で静止。速度を殺して眠らせる。弾けるのはこの状態だけ。
3. **falling** — 隙間から落ちて 1 段下へ向かっている。着地したら rolling に戻る。
4. **lost** — 穴に入った。沈む演出のあとリザルトへ。
5. **win** — あたりの口へ。紙吹雪のあとリザルトへ。

#### Matter.js を使ううえで必ず守ること

実測で分かった、結果が単調でなくなる原因と対処。数値を変えるときは必ず守る。

| 落とし穴 | 対処 |
|---|---|
| Matter の摩擦は接線速度が小さいと**速度をまるごと消す**近似が入る。0.02 でも実測 2700 px/s² という減速になった | **摩擦は 0**。減速は `g・sinθ` ちょうどになる(plan §4.2 の「摩擦を入れない」とも一致) |
| Matter に真円は無く、円は多角形。`Bodies.circle` は辺の数を**奇数**(25)にするので当たり判定が左右非対称になり、奇数段と偶数段で成功域がずれる | 辺の数を**偶数で固定**する(`COIN_SIDES`) |
| コインを回すと、平らな床の上で「角ばった車輪」になって毎秒 160 回上下に振動する。振幅は 0.14px でも、レール端を離れる瞬間の上下速度が −5..−120 px/s の範囲でばらつき、落下位置が数十 px 動く | **物理では回さない**(角速度は常に 0)。見た目の回転は進んだ距離から描画側で作る |
| 構えの位置が 1px でも浮いていると、弾いた直後に落ちて跳ね、その跳ねの位相が結果を変える | 理想座標へスナップせず、**実際に少し物理を回して落ち着かせてから**眠らせる |
| 高い端の壁で跳ね返って戻り、隙間や手前の穴に落ちて「強すぎたのに助かる」が起きる | 隙間の終わりを**中心が越えた時点で強すぎを確定**させる。その先には床が無いので、待つ意味がない |
| サブステップは 1/300 秒あり、900 px/s では 1 歩 3px。境目が 3px ぶんガタつく | 落とし口に沈んだ瞬間を**線形補間**して求める |

- **跳ね返らせない**(反発係数 0)。跳ねると「強すぎたのに戻ってきて助かる」が起き、
  *強すぎ = 失敗* というルールが崩れる。
- 座標・物理定数はすべて `config.ts` に集約する。

### 4.3 弾き力と結果の対応

| 引き量 | 結果 |
|---|---|
| 弱すぎ | **手前の穴**に落ちて没収 |
| 適正 | **隙間から落ちて 1 段下のレーンへ進む** |
| 強すぎ | 隙間を飛び越して**奥の穴**に落ちて没収 |

- **両方向に失敗があること**が本ゲームの本質。難易度によらず必ず両方成立させる。
- 5 つの遷移(段 1→2 … 段 5→あたりの口)は、レーンをすべて合同に作るので
  **完全に同じ条件**になる。1 回目の手応えをそのまま 5 回使える。
- **成功域は連続した 1 区間**でなければならない。
  「ここは通るがもう少し引くと通らない、さらに引くとまた通る」は禁止。

### 4.4 プランジャー(唯一の操作)

- 位置: 画面右下。掴み領域は操作部のほぼ全体(390 × 280 px)。
- Pointer Events(`pointerdown/move/up/cancel`)でタッチ・マウス両対応。
  - つまみを押さえて**下方向にドラッグ**する。**横方向の移動は完全に無視する。**
  - 指の移動量 **240px** / ノブの見た目の移動量 **178px**。
  - 指を離した瞬間(`pointerup` / `pointercancel` どちらでも)に発射。
  - `power = P_MIN + (P_MAX − P_MIN) × pull`。
  - 引き量が極小(`pull < PULL_DEADZONE`)なら発射せず、つまみが戻るだけ。
- 引いているあいだ、盤面の**全レバーが連動して壁の中へ引っ込む**(タメの表示)。
- 発射時は**全レバーが同時に**伸びてコインを蹴る(約 0.2 秒)。
- 弾けるのはレバーで静止しているコインだけ(`canFlick`)。
  コインは必ずレバーで止まるので、待てば必ず弾ける状態になる。**時間制限は無い。**
- 連打制限: 発射後 0.3 秒は再発射不可。

### 4.5 難易度 — 手前の穴の幅だけで差をつける

弾く力の範囲・ストローク・レーンの形はすべて共通にし、**手前の穴の幅だけ**を変える。
幅が広いほど飛び越すのに要る勢いが大きくなり、そのぶん隙間が狭くなって成功域が縮む。

| | やさしい (CASUAL) | ほんき (EXPERT) |
|---|---|---|
| 手前の穴 | 120px | 163px |
| 隙間 | 80px | 37px |
| 成功域 | **55.9%** | **26.1%** |
| 弱すぎ / 強すぎ | 24.8% / 19.3% | 54.7% / 19.3% |

- **合格基準**: 全 5 段について、成功域が やさしい 50〜65% / ほんき 25〜40%(ストローク比)
  であり、かつ**連続した 1 区間**であること。両側に失敗が残っていること。
- レバーの位置は穴の幅に依存しないので、難易度を変えても弾く点は動かない。

---

## 5. ビジュアルデザイン指針

- **すべて Canvas 2D のコード描画で完結させ、外部素材は一切使わない。**
- 世界観: **据置アーケード筐体**。暗い部屋に置かれた金属の箱、真鍮のベゼル、
  その内側のガラス窓が盤面。大人が長く眺めても疲れない暗色ベースにする。
- 情報の伝え方は**色だけに頼らない**:
  - 進める隙間 → 青緑 + **下向きの矢羽根が流れる** + 光の柱
  - アウトの穴 → 橙 + **斜線で塞がれている**
  - 降りた段 → ベゼルに刻んだ目盛りが光る
- レール: 真鍮。天面にハイライト、登る向きの刻み。
- コイン: 金色のメダル。縁の刻みが回転し、中央にどうぶつの顔。
- あたりの口: 盤面の下、最下段の隙間から落ちたコインが飛ぶ側の壁ぎわ。
- 未操作が 3 秒続いたら、指アイコンがノブを下へ引く**操作ガイド**をループ表示する。

## 6. メダル収集仕様

- あたり 1 回につきメダル 1 枚。
- **どうぶつ 10 種**(うさぎ、くま、ぱんだ、りす、ねこ、いぬ、ぞう、きりん、ぺんぎん、らいおん)を
  順番に付与し、10 枚を超えたら 2 周目(同じ順)。
- 保存: `localStorage`。キー `flickshot.save.v1`、値は `{ "stampCount": 12, "lastDifficulty": "easy" }`。
  - 獲得順は種類テーブルから `index % 10` で導出できるので、カウントのみ保存すれば足りる。
  - localStorage が使えない環境では例外を握りつぶし、セッション内メモリのみで動作させる。
- 表示: タイトル画面のメダル棚、あたり時リザルトの押印アニメ。
- **いま挑戦しているコインの意匠は、次にもらえるメダルのどうぶつ**にする。

---

## 7. 技術仕様

### 7.1 スタック

| 項目 | 選定 | 理由 |
|---|---|---|
| ビルド | **Vite** | GitHub Pages 向け静的ビルドが簡単 |
| 言語 | **TypeScript**(strict) | 引き継ぎ実装の安全性 |
| 描画 | **Canvas 2D API** | 依存ゼロ。パーティクルも十分賄える |
| 物理 | **Matter.js**(§4.2) | 盤面を実体として組めば、判定を書かずに済む |
| ランタイム依存 | **Matter.js のみ** | それ以外は入れない |
| テスト | **Vitest** | 物理・判定ロジックの回帰防止 |
| 状態管理 | 自作シーンマシン | Title / Game / Result の 3 シーンのみ |

### 7.2 ディレクトリ構成

```
/
├─ index.html
├─ vite.config.ts               # base: './'(相対パス。§7.5)
├─ plan.md                      # 本書(唯一の仕様)
├─ docs/archive/                # 保管用。仕様として読まない
├─ scripts/verify-geometry.ts   # 盤面の検算(npm run verify)
├─ .github/workflows/deploy.yml
└─ src/
   ├─ main.ts                   # 起動、シーンマシン、メインループ
   ├─ config.ts                 # ★全チューニング定数と色
   ├─ scenes/                   # title / game / result
   ├─ game/
   │   ├─ board.ts              # 盤面の幾何(物理も描画も無い純関数)
   │   ├─ world.ts              # Matter.js の世界を組む
   │   ├─ coin.ts               # コインの状態機械
   │   ├─ plunger.ts            # プランジャー入力
   │   └─ levers.ts             # レバー群のアニメ
   ├─ render/
   │   ├─ layout.ts             # 720×1280 → 実画面フィット、DPR 対応
   │   ├─ cabinet.ts            # 筐体・ベゼル・ガラス・操作部の台
   │   ├─ playfield.ts          # レール・落とし口・レバー・コイン・あたりの口
   │   ├─ hud.ts                # パワーメーター・プランジャー・ボタン
   │   ├─ panels.ts             # ロゴ・難易度カード・メダル棚・はしご
   │   ├─ animals.ts            # どうぶつの顔
   │   ├─ shapes.ts             # 図形・文字・グラデーションの下請け
   │   └─ particles.ts          # 紙吹雪・土煙・火花
   └─ save.ts                   # localStorage ラッパー
```

### 7.3 入力まわりの必須実装

- Pointer Events を使用(Touch/Mouse を個別実装しない)。
- Canvas 要素に `touch-action: none`。
- `<meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">`。
- iOS Safari のダブルタップズーム・長押し選択の抑止(`user-select: none` 等)。
- `pointercancel` でも**発射扱いにする**。
- あきらめるの長押しは進捗リングを描画し、0.8 秒到達で発動。
  途中で指が離れる / 判定円の外に出たらキャンセル。

### 7.4 対応環境・非機能要件

- 対応ブラウザ: iOS Safari / Android Chrome / デスクトップ Chrome・Edge・Firefox・Safari の各最新版。
- 60fps 目標(固定タイムステップ 60Hz + rAF 描画)。
- 通信: 初回ロード以外ネットワーク不要。外部リソース読み込みゼロ。
- 情報を色だけに頼らない(§5)。
- PWA 化・音・多言語・ランキングは**スコープ外**。

### 7.5 GitHub Pages デプロイ(GitHub Actions)

- リポジトリ: `sakanayuki/cc_flickshot` → 公開 URL は `https://sakanayuki.github.io/cc_flickshot/`。
- `vite.config.ts` の `base` は **`'./'`(相対パス)**。絶対パスにすると、配信パスが
  1 文字でも違ったときに JS が 404 になり、**画面が背景色だけになったまま何も出ない**(§11)。
- 起動に失敗したら必ず画面に理由を出す(`showFatal`)。描画ループも try/catch で囲む。
- `.github/workflows/deploy.yml`: `push` to `main`(+ `workflow_dispatch`)。
  `npm ci` → `npm run verify` → `npm test` → `npm run build` → Pages へ。
- **手動設定が 1 つ必要**: Settings → Pages → Source を「GitHub Actions」にする。

### 7.6 テスト方針

Vitest で最低限これを見る(描画はテストしない):

- 盤面の幾何: 5 段が合同で、低い端が交互で、画面の端まで伸びている。
- 弾き: 構えているコインだけ弾ける。斜面に沿った初速になり、重力で減速する。
- 成功域: 弱すぎ → 成功 → 強すぎ が**1 区間ずつ**並ぶこと、5 段で一致すること、空振りが無いこと。
- 段の移り: 隙間から落ちて 1 段下に着地し、その位置が**手前の穴より低い側**であること。
- 投入直後の安全性: 一度も弾く前に落ちない。
- 頑健性: どの引き量でもコインが盤面外に出ない、必ず決着する、座標が壊れない。
- save.ts: 保存・読込・localStorage 例外時のフォールバック。

加えて `npm run verify` で盤面全体を検算する。**このスクリプトは式だけで判断しない。
本番とまったく同じ Matter.js を回して測る。** 計算上は正しいのに遊べない不具合を
何度も作り込んだため(§11)。

---

## 8. 実装ステップ(推奨順序)

1. **足場**: Vite+TS、レイアウト(720×1280 フィット + DPR)、シーンマシン。
2. **盤面**: `board.ts` に幾何、`world.ts` に Matter の世界、`coin.ts` に状態機械。
3. **検算**: `npm run verify` で成功域を測り、手前の穴の幅と `P_MIN`/`P_MAX` を詰める。
   §4.2 の落とし穴の表を先に読むこと。
4. **プランジャーとレバー**: ドラッグ入力、pull→power、全レバー連動。
5. **描画**: 筐体 → 盤面 → 落とし口 → レール → コイン → ガラス → 操作部。
6. **ゲームフロー**: 投入アニメ → プレイ → 3 種の終了 → リザルト。
7. **メダル**: `save.ts`、獲得演出、タイトルのメダル棚。
8. **通しプレイ**: 実機サイズ(縦持ちスマホ)で必ず確認する。
9. **デプロイ**: workflow、Pages 公開、README 整備。

## 9. 受け入れチェックリスト

- [x] スマホ縦持ちで、盤面全体とプランジャーが 1 画面に収まり操作できる
- [x] 盤面が画面の 74%、操作部が 22% に収まっている
- [x] プランジャーは指の移動量 240px をドラッグでき、引き量に応じて弾く力が変わる
- [x] 発射で 5 段すべてのレバーが同時に動く
- [x] コインは**右上**から投入され、上から下へ降りていく
- [x] **弱すぎても強すぎても穴に落ちて没収される**(両方向に失敗がある)
- [x] 成功域が**連続した 1 区間**で、5 段すべて一致している
- [x] 弾いても何も起きない「空振り」がストローク上に無い
- [x] コインが盤面から出ない / 詰まない(全パワー掃引で自動チェック)
- [x] あたりの口に入れたときだけクリアになる
- [x] メダル投入アニメ→プレイ→(穴/あたり/あきらめ)→リザルトの遷移がすべて機能する
- [x] あきらめるは長押しでのみ発動し、誤タップでは何も起きない
- [x] 「やさしい」の成功域が 50〜65%、「ほんき」が 25〜40%
- [x] パワーメーターに直前までのショットの跡が残る
- [x] あたりでメダルが増え、リロード後もタイトル画面に残っている
- [x] 音は一切鳴らない
- [x] 外部素材(画像・音・フォント・CDN)への参照が一切ない
- [ ] `https://sakanayuki.github.io/cc_flickshot/` で動作する(main への push で自動デプロイ)

## 10. 未決事項(実装者の裁量に委ねる範囲)

- どうぶつの具体的なデザイン(§5 の指針の範囲内で自由)。
- 演出時間(指定値は目安。±50% は裁量)。
- `config.ts` 内の物理定数の初期値(§4.5 の合格基準を満たすよう調整する前提)。
- これら以外の仕様変更(画面追加、操作方式変更、音の追加など)は**発注者確認なしに行わないこと**。

---

## 11. 改訂履歴

### 2026-08-01(9) — plan.md を正に、物理を Matter.js に、年齢ターゲットを撤廃

発注者より 3 つの前提が示され、ゲーム画面を 1 から作り直した。

> plan.md が正しく、detailed-design.md を無視する。
>
> また plan のなかでも独自物理エンジンではなく matter.js などを使って駆動する仕様にすること。
>
> 3歳児向けに特化せず大人も楽しめるよう、年齢のターゲットは撤廃すること。
>
> 上記の 3 つの前提のもと、ゲーム画面を、あなたが思う最高で素晴らしい UI で 1 から作り直して。

#### 1. 文書の扱い

旧「詳細設計書」は [`docs/archive/detailed-design-v1.md`](docs/archive/detailed-design-v1.md)
へ移し、冒頭に「仕様として読まないこと」と明記した。本書が唯一の仕様になった。
消さずに残したのは、過去にどんな設計を試して**なぜ捨てたか**が分からなくなると、
同じ袋小路に戻る危険があるため。

#### 2. 物理を Matter.js に置き換えた

盤面を静的剛体として組み、穴と隙間は**単に床が無い区間**として表現した。
判定用の特別なルールは持たない。落ちるか渡りきるかは剛体シミュレーションが決める。

これで自作物理のときにあった「式の上では正しいのに遊べない」がほぼ消えた代わりに、
**エンジン固有の落とし穴**が新しく 6 つ出てきた。すべて実測で見つけて潰し、
§4.2 の表に残した。特に効いたのは次の 2 つ。

- **コインを物理で回してはいけない。** Matter に真円は無く円は多角形なので、
  回すと平らな床の上で「角ばった車輪」になって毎秒 160 回上下に振動する。
  振幅は 0.14px しかないが、レール端を離れる瞬間の上下速度が −5..−120 px/s の幅で
  ばらつき、落下位置が数十 px 動いた。見た目の回転は描画側で作れば絵は変わらない。
- **摩擦は 0 にする。** Matter の摩擦は接線速度が小さいと速度をまるごと消す近似が
  入っていて、0.02 でも実測 2700 px/s² という減速になった。0 にすると
  減速は `g・sinθ` ちょうどになり、plan §4.2 の「摩擦を入れない」とも一致する。

#### 3. 盤面の構造を変えた — 穴と穴のあいだのレールを無くす

Matter に載せ替えた最初の版は、パワーに対する結果が飛び地だらけだった
(`wtwtttttsttwstttstst…`)。軌跡を出して原因が分かった。

> 手前の穴を飛び越えたコインが、その先のレールの**縁に激突**して
> 943 px/s → 402 px/s まで失速していた。

失う量は当たりどころ次第なので、少し強く弾いたほうが手前の穴に落ちる、が普通に起きる。
**穴と穴のあいだのレールを無くし、レールの端を離れたあとは何にも触れない**ようにしたら、
落下位置が単調な式になり、弱すぎ → ちょうど → 強すぎ がきれいに並んだ。

| 論点 | 前 | 今回 |
|---|---|---|
| 床 | レール → 穴 → レール → 隙間 → 穴 | **レール 1 本だけ**。その先はひと続きの落とし口 |
| 穴と隙間 | 別々の開口 | 落とし口を u の範囲で 3 つに区切ったもの |
| 判定 | 「速さがしきい値を割ったら落ちる」独自ルール | **落ちた位置**だけ。ルールは持たない |
| レーンの端 | 壁から 40px 内側 | **画面の端まで**(死角に落ちて詰むのを防ぐ) |
| 結果の並び | 飛び地あり | **弱すぎ / ちょうど / 強すぎ の 3 区間**ちょうど |

「レーンの端と壁のあいだの死角に落ちて詰む」は、この作り直しで新しく出た不具合。
隙間から落ちたコインは勢いを持ったまま高い端の側へ飛ぶので、そこに空きがあると
コインが挟まって弾くことも結果を出すこともできなくなった。端まで伸ばして潰した。

#### 4. 年齢ターゲットの撤廃にともなう変更

| 論点 | 前(3歳児向け) | 今回 |
|---|---|---|
| 見た目 | 絵本調。空色・パステル・太い輪郭 | **据置アーケード筐体**。暗色 + 真鍮 + ガラスの映り込み |
| 難易度の名前 | やさしい / ふつう | **やさしい (CASUAL) / ほんき (EXPERT)** |
| 成功域の目標 | やさしい 60% 以上 / ふつう 30〜50% | **やさしい 50〜65% / ほんき 25〜40%** |
| 上達の手がかり | 無し | **パワーメーターにショットの跡が残る**(よわい / ちょうど / つよい) |
| リザルト | 結果だけ | **弱すぎ / 強すぎ と、次にどう直すか**、最後に使った引き量 % |
| 難易度の選び方 | 文字だけ | **カードにレーンの縮図**を描き、穴と隙間の広さを図で見せる |

「答えを配らない」ことは保っている。成功する範囲そのものは表示せず、
自分が撃った跡だけを見せて詰めさせる。

#### チューニング結果

| | やさしい | ほんき |
|---|---|---|
| 手前の穴 | 120px | 163px |
| 隙間 | 80px | 37px |
| 弱すぎ / 成功 / 強すぎ | 24.8% / **55.9%** / 19.3% | 54.7% / **26.1%** / 19.3% |
| 成功域の連続性 | 1 区間 | 1 区間 |
| 5 段の一致 | 一致 | 一致 |
| 空振り | 0 件 | 0 件 |

レーン長 642.8px / 傾き 5.4° / レール 0..300 / 落とし口 300..643 / 弾く力 525..1096 px/s。
検算 114 項目、ユニットテスト 72 件がすべて通っている。

### 2026-07-31(8) — レーンはななめ上向き。コインは登り、隙間から落ちて下の段へ

発注者より、作っているものが実機と違うとの指摘があった。

> 筒みたいな全然違うゲームになってる。
> ○重力は画面下向きに働いている
> ○レーンがななめ上向きにある
> ○バーを弾くことでコインに勢いがつき、このレーンを登ることができる。
> ○勢いが強すぎたり弱すぎるとアウトになるような、レーンに隙間や穴がある。
>   適切な力で弾いたときの隙間から落ちたときだけ下の段に進める

#### 原因 — 「下る」しか考えておらず、重力を横向きの摩擦に置き換えていた

改訂(7)は「1本につながったレーンを走る」形にしたが、これは
**レーンをつなげて筒のようにしてしまい**、実機の「段になったななめのレーンを
登り、隙間から落ちて下の段へ移る」という構造とは別物だった。
重力も画面下向きには効いておらず、進行方向の摩擦として扱っていた。

#### 対処 — 重力を画面下向きに戻し、レーンをななめ上向きの段にする

| 論点 | 変更 |
|---|---|
| 重力 | 進行方向の摩擦(LANE_DRAG) | → **画面の下向き**。斜面の減速は `GRAVITY * sinθ` として自然に出る |
| レーン | Uターンでつながった 1 本 | → **ななめ上向きのレーンが 5 段**。つながっていない |
| 弾いたあと | 走って減速し、止まり木で止まる | → **レーンを登る**。登りきれなければ滑り降りてくる |
| 段の移り方 | Uターンを回って次の走路へ | → **隙間から落ちて 1 段下のレーンへ** |
| 弱すぎ | 穴を渡りきれない | → **手前の穴**を渡りきれずに落ちる |
| ちょうど | 止まり木に受け止められる | → **隙間から落ちて次の段へ進む**(ここが発注者の指摘の芯) |
| 強すぎ | 止まり木を乗り越える | → **隙間を飛び越して奥の穴**へ |
| 難易度差 | 穴の長さ | → **手前の穴の長さ**(短いほど渡りやすく、隙間も広い) |

レバーが画面の左右の端にあること(改訂(6))と、プランジャー 1 操作で
全レバーが連動する仕様はそのまま維持している。

#### 摩擦を入れないことにした

斜面の運動に摩擦を入れると、登りと下りが非対称になって挙動が読みにくくなる。
摩擦なしの等加速度にすると、成功する初速が**閉じた式**で書ける。

```
手前の穴を渡りきる : v0² >= 落ちる速さ² + 2 * 斜面の減速 * 手前の穴の終わり
隙間で落ちる       : v0² <  落ちる速さ² + 2 * 斜面の減速 * 隙間の終わり
```

チューニングはこの式で当たりをつけ、**式と実物理が一致すること自体を検算に入れた**。
片方だけ壊れても気づける。

#### 新しく生まれた失敗の芽と、その潰し方

1. **着地したコインが自分から手前の穴に落ちる。**
   隙間から落ちたコインは 1 段下のレーンに乗って滑り降りるので、
   着地点が手前の穴より奥だと、滑り降りる途中で落ちてしまう。
   隙間をレーンの高い端の側に置くことで、着地点が必ず低い端の側に来るようにした。
   検算が「着地 u < 手前の穴の位置」を数値で固定している。
2. **弾いても何も起きない「空振り」。**
   手前の穴に届かない弱さで弾くと、コインは滑り降りてレバーに戻るだけになる。
   3歳児には理由が分からないので、`P_MIN` を「最弱でも手前の穴には必ず届く」
   値より上に置き、検算で空振りが 0 件であることを確認している。

#### チューニング結果

| | やさしい | ふつう |
|---|---|---|
| 手前の穴の長さ | 60px | 130px |
| 各操作の成功域 | 63%(全 5 回とも同じ) | 32%(全 5 回とも同じ) |
| 成功域の連続性 | すべて1区間 | すべて1区間 |
| 弱すぎ・強すぎの失敗 | 両方あり | 両方あり |
| 空振り | 0 件 | 0 件 |

### 2026-07-31(7) — コインは飛ばない。レーンの上を走る

発注者より、弾いたあとの動きが違うとの指摘があった。

> 弾く機能は、玉が飛ぶというより、勢いがついてレーンの上を沿って走るイメージです。
> この青レーンを再現するように忠実にしてください

#### 原因 — 「飛ばす」を前提に盤面を組んでいた

改訂(6)までは、コインを放物線で飛ばして 1 段下のレールに着地させる設計だった。
そのため盤面は**切れ切れのレール**の集まりで、写真の青い線のような
「1 本につながったレーン」にはならない。弾道が主役である限り、
レーンの形は自由に引けなかった。

#### 対処 — 物理をレーンに沿った 1 次元に置き換える

| 論点 | 変更 |
|---|---|
| コインの動き | 放物線で飛ぶ | → **レーンに沿って走る**。位置は経路上の距離 `s`、速度は前向きの速さ `v` だけ |
| 盤面 | 切れ切れのレール 5 本 | → **1 本につながった折れ線**(直線 + 壁でのUターン)。写真の青い線そのもの |
| 弾く | 仰角つきの初速を与える | → レーンに沿った初速を与えるだけ。**仰角という概念が無くなった** |
| 止まる場所 | レールの先端の溝 | → 走路の端(画面の左右の端)の**止まり木**。軸が筐体を貫いて横の壁のレバーになる |
| 穴 | レールとレールの隙間 | → **レーンの床に開いた丸い口**。Uターンを出てすぐの位置 |
| 弱すぎ | 隙間を飛び越せない | → **穴の上で勢いを失って落ちる**(勢いがあれば口を渡りきれる) |
| 強すぎ | 板を飛び越して奥の穴 | → **止まり木を乗り越えて**その先の穴に落ちる |
| 難易度差 | 板の長さ | → **穴の長さ**(短いほど助走が長く取れて成功域が広い) |
| 当たり判定 | 全レールの実体(上面+厚み+端面) | → 不要。1 次元なので貫通も場外も原理的に起きない |

弾く点が画面の左右の端にあること(改訂(6)の要望)と、
プランジャー 1 操作で全レバーが連動する仕様はそのまま維持している。

#### 新しく増えた失敗の芽と、その潰し方

1 次元にしたことで貫通と場外は消えたが、代わりに
**「コインがレーンの途中で完全に止まって詰む」**が起こりうる。
弾くこともできず結果も出ないので、ゲームが進行不能になる。

レーンにわずかな傾き `LANE_ASSIST` を持たせ、止まりかけたコインを
ゆっくり前へ送り続けることで潰した。止まりかけの速さ `LANE_CREEP` は
穴に必ず捕まる速さ(`< HOLE_CATCH_SPEED`)にしてあるので、
「止まりかけて穴の手前で永久に静止する」も起きない。
検算スクリプトが全パワーで「必ず決着する」ことを確認する。

#### チューニング結果

| | やさしい | ふつう |
|---|---|---|
| 穴の長さ | 120px | 190px |
| 各操作の成功域 | 65%(全 5 回とも同じ) | 48%(全 5 回とも同じ) |
| 成功域の連続性 | すべて1区間 | すべて1区間 |
| 弱すぎ・強すぎの失敗 | 両方あり | 両方あり |
| 5 回の操作の均一性 | 距離が完全に一致 | 同左 |

### 2026-07-31(6) — レーンの引き直しと、弾く点を画面の左右の端へ

発注者より、実機の写真に青いレーンを描き込んだものとともに指摘があった。

> ・添付画像の青いレーンのようなレーンを設計しなおしてください。
>   レーンが丸くなっているところには穴があり、ここにコインがはいるとアウトになります。
> ・コインをはじくポイントが、今の実装だと画面中央付近になっているので、添付画像のように
>   青いレーンと左右の画面端に面しているところの横のレバーではじくような仕組みにしてください。
>   (※このWebアプリでは、このレバーをはじく部分は画面右下の1操作で統一です。
>   これはこのままの仕様にしてください。)

#### 原因 — 前回の対処が「弾く点を端に置く」ことと両立していなかった

改訂(4)は貫通を消すために「溝は板の**先端**、弾く向きは**先端の外**(板から離れる向き)」
という幾何を採った。この形だと

```
溝から壁までの距離 = 板の幅 + 奥の穴の幅
```

となり、弾く点は必ず盤面の中央寄りに固定される。改訂(5)で隙間を広げて
190px まで寄せたのが限界で、**この前提を裏返さない限り端には置けなかった**。

#### 対処 — 弾く向きを裏返し、飛び越せることを検算で担保する

| 論点 | 変更 |
|---|---|
| レーンの配置 | 中央を挟んだ2本の柱 → **左端・右端・左端…と交互**。写真の青いレーンと同じ並び |
| 弾く点(溝) | x=230 / 490(壁から 190px) → **x=124 / 596(壁から 84px = 盤面幅の 13%)** |
| レバー | 先端の真下のハンマー → **先端の横、壁から生える**。軸が筐体を貫いて左右のノブになる(写真と同じ) |
| 弾く向き | 先端の外 → **盤面の内側**。コインは自分のレーンを飛び越して飛ぶ |
| 穴 | 溝と溝の間 → **盤面の中央に丸を並べて開口を埋める**+**先端と壁のあいだ** |
| 高い端の返し | あり(12px) → **廃止**。あると自分のレーンを飛び越せない |
| 仰角 | 28° → **35°**(下限は自分のレーン、上限は1段上のレーンの裏で挟まれる) |
| 板の幅 | 120 / 80 → **140 / 100** |
| 弾き力 | 530..840 → **665..950 px/s** |

操作は指定どおり画面右下のプランジャー1つのまま。全段のレバーが連動する仕様も変えていない。

#### 貫通(改訂(4))を再発させないための担保

新しい弾道は、改訂(4)が「構造的にありえない」と書いた**自分のレーンの上を飛ぶ形**になる。
今回は幾何で消すのではなく、成立条件を数式で書いて機械で守ることにした。

```
gain(w) = tan(仰角)·w − g·w² / (2·vx²)  >  PLANK_DROP     (レール全長にわたって)
頂点の高さ = vy² / (2g)                  <  1段上のレールの裏まで
```

検算スクリプトに **§8「弾道が自分のレールと1段上のレールを避けること」** を追加した。
物理エンジンを通さない理想の放物線で余裕を px で測る。エンジンの押し出しに隠れて
「当たっているのに §7 は通る」状態を検出できるようにするため、§7 とは別に持っている。

#### チューニング結果

| | やさしい | ふつう |
|---|---|---|
| 板の長さ | 140px | 100px |
| 各遷移の成功域 | 65% / 65% / 65% / 65% / 68% | 45% / 45% / 45% / 45% / 48% |
| 成功域の連続性 | すべて1区間 | すべて1区間 |
| 弱すぎ・強すぎの失敗 | 両方あり | 両方あり |
| 飛行中の板へのめり込み | **0.0px** | **0.0px** |
| 自分/上のレールとの余裕 | **14.1px** | **14.6px** |

### 2026-07-28(5) — 弾く点を左右の端へ、穴を「棒と棒の間」全体へ

発注者より、配置が実機と違うとの指摘があった。

> ゲーム画面の、棒と穴の配置がダメだ。まだ実機と違うと自覚している点を実機寄りにして。
> 操作感はそのままでよい。
> また、弾くポイントが中央に寄ってしまってるが、実機の通り左右の端側にあるはずだ。

#### 判明した制約

弾く点(溝)は棒の先端にあり、棒はそこから壁へ向かって伸びる。
その先に「強すぎ」で落ちる奥の穴が要る。したがって

```
溝から壁までの距離 = 棒の幅 + 奥の穴の幅 = (640 − 中央の隙間) / 2
```

**溝を壁へ寄せるには、中央の隙間を広げて棒を細くするしかない。**
棒を太いままにすると溝は必ず中央付近に留まる。これが「中央に寄ってしまう」原因だった。
逆に棒を細くしすぎると奥の穴が消え、「強すぎ」で失敗できなくなる。

総当たり(仰角5種 × 隙間12種 × 棒幅15種、実物理で成功域を二分探索)で
1913 件の成立解を測り、棒がコイン直径(56px)に対して十分な幅を保てる範囲で
最も端に寄る設定を採った。

#### 変更点

| | 前 | 今回 |
|---|---|---|
| 溝(弾く点)の x | 300 / 420 | **230 / 490** |
| 溝から壁まで | 260px | **190px** |
| 中央の隙間(間の穴) | 120px | **260px** |
| 奥の穴 | 60px | **70px** |
| 棒の幅(やさしい/ふつう) | 190 / 110 | **120 / 80** |
| 弾き力 | 250..800 | **530..840** |
| 間の穴の見た目 | 直径88pxの丸穴 | **隙間をまるごと開口**(240×68の楕円) |
| レバーのノブ | 無し | **筐体の左右の縁に段ごと交互**(実機と同じ) |

- 間の穴を隙間いっぱいに開けたのは、発注者の「棒と棒の間」という指定に
  正確に合わせるため。丸穴だと、落ちる範囲と見た目がずれて
  「板の上に乗ったのに落ちた」ように見える箇所が残っていた。
- 盤面の余白に印刷の草花を足した。コインの通り道(棒の溝側)は避け、
  棒の高い端の外側にだけ置いている。

#### この変更で見つかった潜在バグ

中央の隙間を広げた結果、コインが**棒の左上の角に食い込む**経路が初めて現れ、
検算 §7 が 12px のめり込みを検出した。端面の当たり判定に

```ts
if (p.y < top - COIN_R * 0.35) return false;   // 誤り
```

という除外条件があり、「板面より上なら上面判定に任せる」つもりだったが、
上面判定は板の範囲内(`onPlank`)でしか働かない。範囲外から角に入るコインが
どちらの判定にも拾われず、そのまま板を突き抜けていた。
コインの下端が板面に触れているかで判定するよう修正した。

```ts
if (p.y + COIN_R < top) return false;          // 正
```

前回の狭い隙間では露出しなかった経路で、**検算が無ければ気づけなかった**。

#### チューニング結果

| | やさしい | ふつう |
|---|---|---|
| 棒の幅 | 120px | 80px |
| 各遷移の成功域 | 71% / 71% / 71% / 71% / 64% | 50% / 50% / 50% / 50% / 45% |
| 成功域の連続性 | すべて1区間 | すべて1区間 |
| 弱すぎ・強すぎの失敗 | 両方あり | 両方あり |
| 飛行中の板へのめり込み | 0.0px | 0.0px |

### 2026-07-28(4) — 棒の貫通と、穴のゲーム性・見た目の作り直し

発注者より、実装が遊べる状態になっていないとの指摘があった。

> 棒を貫通するし、穴の見た目はださいし、ゲームとして破綻していて面白くない。
>
> 穴のゲーム性がださいから、以下のように見直して。
> 道中の穴は、棒と棒の間や、棒の端に溝があり、力が弱すぎると間の穴を乗り越えることが
> できず、強すぎると奥の穴に落ちてしまうような仕組み。
> 画像の中のメダルが通る道を考えて確認して。私が書いた通りの仕様になっています。

#### 原因 — 弾く向きが幾何的に破綻していた

前回の設計は「溝は板の低い**側**」「弾く向きは**溝と反対**」だった。
つまりコインは**自分が乗っている板の上を逆走する形で飛び出す**。
板は飛行方向に向かってせり上がっているので、どんな弾道でも板の内部を通る。
これが「棒を貫通する」の正体で、**チューニングでは直せない構造的な欠陥**だった。
着地判定を「1段下の板だけ」に限っていたため、自分の板との衝突が
そもそも計算されず、コインが板をすり抜けて見えていた。

#### 対処

| 論点 | 変更 |
|---|---|
| 盤面の形 | 板を**ジグザグ**に配置。溝は板の**先端**、弾く向きは**先端の外**(板から離れる向き)。飛行中に自分の板を横切ることが構造的に無くなった |
| 衝突 | **すべての板を実体(上面+厚み+端面)として衝突**させる。1サブステップ 6px 以下に分割して積分し、高速でもすり抜けない |
| 端に当たったとき | 跳ね返さず、水平の勢いを失ってそのまま落ちる |
| 穴 | 「板でないところ」という帯 → **丸い落とし穴**。実機と同じオレンジのリム付き。間の穴は溝と溝の真ん中、奥の穴は板の端のすぐ先に置く |
| 落ちる演出 | 穴の口まで滑って沈み、**手前側のリムがコインの上に重なる**。穴に入っていくように見える |
| 仰角 | 12° → **28°**。放物線がはっきり見えて「弾かれた」感が出る |
| 見た目全体 | 画面全体を実機の赤いキャビネットにし、中央のガラス窓が盤面。化粧板・ネジ・コース案内の矢印を追加 |
| レバー | 板の先端の下にぶら下がるハンマーに変更。振り上げてコインを下からはたく。打撃の残像と星を出す |

#### 新しい盤面の数値

```
GROOVE_GAP = 120     // 中央の隙間(=間の穴)。全段共通
偶数段の溝 x = 420(板は右へ伸びる。左へ弾く)
奇数段の溝 x = 300(板は左へ伸びる。右へ弾く)
板幅  やさしい 190 / ふつう 110
弾き力 250..800 px/s、仰角 28°
```

#### チューニング結果

| | やさしい | ふつう |
|---|---|---|
| 板の幅 | 190px | 110px |
| 各遷移の成功域 | 71% / 71% / 71% / 71% / 65% | 45% / 45% / 45% / 45% / 40% |
| 成功域の連続性 | すべて1区間 | すべて1区間 |
| 弱すぎ・強すぎの失敗 | 両方あり | 両方あり |
| 飛行中の板へのめり込み | **0.0px** | **0.0px** |

検算スクリプトに **§7「板を貫通しないこと(全パワー掃引)」** を追加した。
全段・全パワーで飛行中の全サブフレームを走査し、コインの円が板の実体に
食い込んでいないことを見る。同じ内容を Vitest 側でもテストしている。

### 2026-07-28(3) — ルールの全面見直し「やまのぼり」→「やまくだり」

発注者より、**ゲームそのものを取り違えていた**との指摘があった。

> 根本的にこのゲームそのものを間違えて認識していました。これは上から一番下まで降りるゲームです。
> 適度な強さでコインを弾くと、溝から下の段に進むことができます。強く叩きすぎたり、弱く叩きすぎる
> 場合は穴に落ちてしまい、コインを没収される仕組みです。
> また、バネを画面の35%高さを占めていますが、もっとゲーム画面を大きく表示したいので、割合を小さくして。
> また、コインは右上に入れるものとし、コインは右上から下に進むものとして作り直して。

これまでの実装は「下から上へ登り、強すぎても失敗しない」という**真逆**の設計だった。
物理の中核(弾く方向・成功条件・失敗条件)がすべて反転するため、`game/` と盤面描画を作り直した。

#### 発注者と確定した仕様

| 論点 | 決定 |
|---|---|
| クリア条件 | 最下段の先に**「あたりの口」**があり、そこに入れて初めてクリア。外すと没収 |
| バネの帯 | 画面の **22%**(盤面は 74% に拡大)。指のドラッグ距離は 450px → **240px** |
| やさしいの救済 | **両難易度とも強すぎ・弱すぎの両方で落ちる**が、やさしいは成功域をストロークの **60% 以上**に取る |
| 段数 | **5段のまま**(+ 最後にあたりの口) |

#### 廃止した仕組み

旧ルール固有の以下は全廃した。新ルールでは意味を持たない。

- 上向きの発射シャフト、レーン先端・裏面の当たり判定、リップ越えの転落
- 転がり速度で穴に落ちる判定(`fallSpeed` / `holeRadius` / `lipEscapeSpeed`)
- 壁での跳ね返り(§4.2 のとおり、跳ね返らせるとルールが崩れる)
- ゴールバスケット(→ あたりの口)

#### 新ルールでのチューニング結果

| | やさしい | ふつう |
|---|---|---|
| 板の幅 | 330px | 160px |
| 各遷移の成功域 | 85% / 85% / 85% / 85% / 89% | 43% / 43% / 43% / 43% / 47% |
| 成功域の連続性 | すべて1区間 | すべて1区間 |
| 弱すぎ・強すぎの失敗 | 両方あり | 両方あり |

- `P_MAX` を 1400 にすると、やさしいでもストローク上端が丸ごと「強すぎ」になって成功域が
  52% に落ちた。1000 に下げて 85% を得た。
- 板幅を 340 にすると成功域は 87% になるが、**「強すぎ」の失敗が消滅**した
  (板が壁に接してしまい、強く弾いても壁で止まって板に戻る)。
  これは発注者の「両方で落ちる」という指定に反するため、330 に戻した。
  この違反は検算スクリプトの「弱すぎでも強すぎでも落ちる」チェックが自動で検出した。

### 2026-07-28(2) — 公開ページが真っ青になる不具合

発注者より「`https://sakanayuki.github.io/cc_flickshot/` にアクセスしましたが、
真っ青で何も表示されません」との報告。

原因は `vite.config.ts` の `base: '/cc_flickshot/'`(絶対パス)。配信パスが想定と違うと
JSが404になり、Canvasが初期サイズ(300×150)のまま残って body の背景色だけが見える。
**エラーも何も表示されないため、端末側からは原因が全く分からない。**

対処(いずれも再発防止として恒久化):

- `base: './'` に変更。どこから配信されても解決できるようにした
- 起動に失敗したら画面に理由を出す `showFatal()` を追加
- 描画ループを try/catch で囲み、描画で落ちたときも理由を出す
- `<noscript>` を追加
- `modulePreload: false`(バンドルから唯一の `fetch()` も消えた)
- `.nojekyll` を追加

### 2026-07-28(1) — 詳細設計書の作成にともなう追加確定

詳細設計書(現 [`docs/archive/detailed-design-v1.md`](docs/archive/detailed-design-v1.md))を作成し、
操作ガイド(未操作2秒ごとに毎回表示)などを追加確定した。

このとき「座標の計算は正しいのに遊べない」不具合を繰り返し作り込んだため、
検算スクリプトを**放物線を解く方式から、実際の物理を回して測る方式に変更**した。
この方針は新ルールでもそのまま引き継いでいる。
