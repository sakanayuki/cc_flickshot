# plan.md — コインフリックゲーム「どうぶつの やまくだり」要件定義書

このドキュメントは発注者との対話で確定した**要件**(何を作るか)を記載している。

> **⚠️ 実装者へ: 実装時は [`docs/detailed-design.md`](docs/detailed-design.md)(詳細設計書)を参照すること。**
> 座標・物理定数・型定義・関数シグネチャはすべてそちらに確定値で書かれている。
> **具体的な数値が本書と詳細設計書で食い違う場合、詳細設計書を正とする。**
> 本書は「なぜそうなっているか」という背景と意図を理解したいときに読む。

> **⚠️ 2026-07-28、ゲームのルールを全面的に見直した。**
> 当初は「下から上へ登る」ゲームとして設計・実装していたが、発注者の意図した実機は
> **「上から下へ降りる」**ゲームだった。物理・盤面・判定のすべてが反転している。
> 経緯は §11 を参照。本書はすでに新ルールで書き直してある。

---

## 1. コンセプト

- 昔の駄菓子屋にあった「10円ゲーム機」(新幹線ゲーム系のエレメカ筐体)のWebアプリ版。
  実機では、右上から投入したコインが段になった板の上に乗り、プレイヤーがレバーで
  コインを弾いて1段ずつ下へ降ろしていく。**適度な強さで弾けば下の段に乗るが、
  強すぎても弱すぎても穴に落ちてコインが没収される。**
  最下段のさらに下にある「あたりの口」に入れられたらクリア。
- **ターゲットは3歳児**。保護者と一緒に、または一人で遊べること。
  すべてのUI・難易度・演出は「3歳児が理解でき、失敗しても泣かない」ことを最優先に設計する。
- テーマは「**どうぶつの やまくだり**」。どうぶつメダルを弾いて下の「あたり」を目指す絵本調の世界観。

### 実機との最重要な差分(発注者指定)

実機ではレバーは1本ずつ独立操作だが、本作では
**画面右下の「引っ張れるプランジャーボタン」1つで全レバーが連動して同時に動く**。

- 引っ張った量が大きいほど、弾く力が強くなる。
- シビアな操作を減らすため、**引っ張れるストロークは長めに確保する**。
  ただし盤面を大きく見せる要求と両立させるため、指の移動量は240px(画面高さの約19%)とし、
  足りないぶんの余裕は**板の幅を広く取ること**で確保している(§4.5)。

---

## 2. 確定済み要件(発注者との合意事項)

| 論点 | 決定内容 |
|---|---|
| ゲームの向き | **上から下へ降りる**。適度な強さで弾けたときだけ1段下の板に乗る |
| 失敗条件 | **強すぎても弱すぎても穴に落ちてコイン没収**。両方向に失敗がある |
| クリア条件 | 最下段の下にある**「あたりの口」**に入れる。外せば没収 |
| 操作方式 | 右下プランジャー1つで全レバー連動。引き量=弾く力。ストローク長め |
| 画面構成 | タイトル画面 / ゲーム画面 / リザルト画面 の3画面 |
| ゲーム開始 | **画面右上の投入口**からのメダル投入アニメーションで始まる |
| リザルト遷移条件 | ①穴に落ちる ②あたりの口に入る ③「あきらめる」長押し の3つのみ |
| デバイス | **スマホ/タブレット縦向き優先**(PCでは中央に縦長キャンバス表示) |
| テーマ | **どうぶつの やまくだり**(絵本調、パステル+はっきりした輪郭) |
| サウンド | **音なし**(BGM・効果音ともに実装しない。ミュートボタンも不要) |
| 難易度 | **2段階「やさしい / ふつう」**をタイトル画面で選択 |
| 盤面の大きさ | **画面の 74%**。プランジャー帯は 22% に抑える |
| 段数 | **5段**(+ 最後にあたりの口) |
| ごほうび | **スタンプ収集あり**。あたりごとにどうぶつスタンプ1個獲得、localStorageに永続化 |
| あきらめるボタン | **左上に小さく配置、1秒長押しで発動**(誤タップ対策。進捗リングを表示) |
| デプロイ | GitHub Actions で GitHub Pages に自動デプロイ |

---

## 3. 画面仕様

### 3.1 共通レイアウト

- 論理座標系: **720 × 1280(9:16 縦)**。全シーンこの座標系で描画する。
- 実画面へは アスペクト比維持でフィット(レターボックス)。余白は背景色(空色)で塗る。
- `devicePixelRatio` を考慮して Canvas の実ピクセルサイズを設定し、にじみを防ぐ。
- 文字は**ひらがな中心・最小限**。可能な限りアイコンと絵で伝える。
- フォントはシステムフォント(`sans-serif` 系)でよい。外部フォント読み込みはしない。

### 3.2 タイトル画面

- 内容:
  - タイトルロゴ「どうぶつの やまくだり」(Canvas描画。下へ降りる段々+コイン+どうぶつの飾り)。
  - 難易度ボタン2つ(大きく、縦に並べる):
    - 「やさしい」(推奨。デフォルトで強調表示)
    - 「ふつう」
  - どちらかをタップすると即ゲーム画面へ(確認ダイアログなどは挟まない)。
  - 画面下部に**スタンプ帳エリア**: 集めたスタンプ数と、獲得済みスタンプを小さく並べて表示。
    0個のときは空の枠を数個見せて収集意欲を誘う。
- 3歳児配慮: ボタンは指が多少ズレても押せるサイズ(論理座標で高さ120px以上)。

### 3.3 ゲーム画面

レイアウト(縦720×1280内):

```
+----------------------------------+
| [あきらめる(小)]      ▼投入口     |  ← 左上は長押しで発動。右上からコインが入る
|  ┌─ 盤面 ────────────────────┐   |
|  │  [穴] ████████████ [穴] 段1 │   |  ← 溝は右端。左へ弾く
|  │  [穴] ████████ [穴]     段2 │   |  ← 溝は左端。右へ弾く    画面の 74%
|  │      [穴] ████████ [穴] 段3 │   |
|  │  [穴] ████████ [穴]     段4 │   |
|  │      [穴] ████████ [穴] 段5 │   |
|  │        [穴] ▼あたりの口 [穴] │   |
|  └────────────────────────────┘   |
|                    [プランジャー] |  ← 右下。画面の 22%。指の移動量 240px
+----------------------------------+
```

- 開始時: **メダル投入アニメーション**(約1.4秒)。
  どうぶつメダルが**右上の投入口**にコトンと入り、シュートを滑って段1の板に出てくる。
  出たコインは板の傾斜で溝まで転がり、そこで止まる。
  この間プランジャーは操作不能(見た目はグレーではなく通常のまま、引いても反応しないだけ)。
- プレイ中: プランジャー操作でメダルを横に弾き、1段ずつ下へ降ろす。
  着地したコインは板の傾斜で必ず溝まで転がって止まるので、**時間的な制約はない**。
  3歳児が落ち着いて狙えることを最優先している。
- 終了条件(いずれかで即リザルト画面へ遷移。遷移前に短い演出を挟む):
  1. メダルが穴に落ちる(落下アニメ約1秒 → リザルト)
  2. メダルがあたりの口に入る(旗が揺れる 約1.5秒 → リザルト)
  3. あきらめるボタン1秒長押し完了(即リザルト)

### 3.4 リザルト画面

結果に応じて3パターンの表示。いずれも**責める演出は絶対にしない**。

| 結果 | 見出し(ひらがな) | 演出 |
|---|---|---|
| あたり | 「やったね!」 | 紙吹雪(Canvasパーティクル)、どうぶつがバンザイ、**スタンプ獲得演出**(スタンプが「ぺたん!」と押されるアニメ) |
| 穴落ち | 「おしい!」 | どうぶつが穴からひょっこり顔を出して笑う。悲しい表現は使わない |
| あきらめ | 「またね!」 | どうぶつが手を振る |

- 共通表示: 今回の**到達段数**を、上から下へ並ぶ段の絵で表示(降りた段だけ木の色で塗る)。
- ボタン:
  - 「もういちど」(大): 同じ難易度で即ゲーム画面へ(メダル投入アニメから)。
  - 「さいしょから」(小): タイトル画面へ。

---

## 4. ゲームデザイン詳細

### 4.1 盤面構成 — 各段は「穴・板・穴」の3ブロック

- 盤面に**5段の板**を上から下へ配置する(段1が最上段)。
- 各段の板は**片側に向かって少しだけ下っている**。低い側の端が「溝(みぞ)」で、
  コインは必ずそこまで転がって止まる。溝の位置は段ごとに右・左・右…と交互。
- 溝の脇に**レバー**がある。プランジャーを離すと全レバーが同時にはたき、
  溝にいるコインを**溝と反対向きへ横に弾き飛ばす**。
- **板の左右は穴**。1段下の板に乗れなければそこへ落ち、コインは没収される。
  - **弱すぎ** → 発射した側に近い「手前の穴」に落ちる
  - **強すぎ** → 板を飛び越えて反対側の「奥の穴」に落ちる
- 最下段のさらに下に**「あたりの口」**がある。段5から弾いてここに入ればクリア。外せば没収。
- 「木の板に乗る / 黒い穴に落ちる」は3歳児にも一目で分かる形にする。

### 4.2 コインの挙動(自作簡易物理)

物理エンジン(Matter.js等)は**使わない**。理由: 3歳児向けに「理不尽な跳ね方をしない、
決定論的で優しい挙動」を作り込む必要があり、自作の単純モデルの方が調整しやすいため。

コインは常に次の4状態のいずれか:

1. **onPlank(板の上)**: 板に沿った1次元運動。
   加速度 = 重力の坂方向成分 − 転がり摩擦(速度に比例)。溝に着いたら速度0で停止。
2. **airborne(空中)**: 弾かれた後の放物運動(重力のみ。空気抵抗なし)。
   **1段下の板(最後はあたりの口)だけを着地面として見る。**
   - 板の範囲内に落ちたら着地 → onPlank へ。跳ねさせない(挙動を読みやすくするため)。
   - 板を外したら穴へ落ちる → falling へ。
3. **falling(穴落下)**: 穴に吸い込まれる演出状態。操作不能。縮小しながら穴中心へ。約1秒でリザルトへ。
4. **win(あたり)**: あたりの口の中で小さく跳ねる演出。約1.5秒でリザルトへ。

- **盤面の側壁では跳ね返らせない**(横速度を0にして滑り落とす)。
  跳ね返らせると「強すぎたのに壁のおかげで板に戻る」ことが起き、
  *強すぎ = 失敗* というルールが崩れるため。
- 更新は**固定タイムステップ(60Hz、accumulator方式)**。描画はrequestAnimationFrame。
- 座標・速度などのチューニング値はすべて1つの定数ファイル(`config.ts`)に集約すること。

### 4.3 弾き力と結果の対応(設計の核)

| 引き量 | 結果 |
|---|---|
| 弱すぎ | 1段下の板に届かず、**手前の穴**に落ちて没収 |
| 適正 | 1段下の板に着地。傾斜で溝まで転がって止まり、次の一手を落ち着いて狙える |
| 強すぎ | 板を飛び越えて**奥の穴**に落ちて没収 |

- **両方向に失敗があること**が本ゲームの本質であり、難易度によらず必ず両方を成立させる。
  片側でしか失敗しないなら、板が壁に接していて設計が崩れている(検証で自動チェックする)。
- 5つの遷移(段1→2、2→3、3→4、4→5、段5→あたりの口)は、
  **横に飛ぶ距離も落差もすべて同一**になるよう座標を逆算して決める。
  こうすることで弾き力の適正範囲が段によってばらつかず、3歳児が「同じ引き方」を再現できる。

### 4.4 プランジャー(唯一の操作)仕様

- 位置: 画面右下。**掴み領域はプランジャー帯のほぼ全体**(幅420×高さ284px)。
  ノブの見た目は小さくても、3歳児がどこを触っても掴める。
- 操作: Pointer Events(`pointerdown/move/up/cancel`)でタッチ・マウス両対応。
  - つまみを押さえて**下方向にドラッグ**するとつまみが下がる。
  - **指の移動量: 240px** / **ノブの見た目の移動量: 190px**(引ききってもノブが画面内に残るため)。
  - 指を離した瞬間(`pointerup` / `pointercancel` どちらでも)に**発射**。
  - 引き量 `pull ∈ [0, 1]` を弾き力にマップ: `power = P_MIN + (P_MAX − P_MIN) × pull`。
  - 引き量が極小(pull < 0.05)のときは発射せず、つまみが戻るだけ(誤タップで空撃ちさせない)。
- つまみはバネ表現で戻る(離した後0.15秒程度で上端へ)。引いている間、盤面の**全レバーが
  連動して引き下がる**(タメの視覚フィードバック)。
- 発射時: **全レバーが同時に**はたきアニメーション(約0.2秒)。
- 弾く向きは溝の反対側。仰角は水平から12°(放物線が見えて「弾かれた」感じが出る程度)。
- 弾きゾーン: 溝から±90px以内にいる `onPlank` のコインだけが弾かれる。
  コインは必ず溝で停止するため、実際には常にゾーン内にいる。
- 連打制限: レバー作動後0.3秒はプランジャー再発射不可(表示は不要)。

### 4.5 難易度 — 板の幅だけで差をつける

弾き力の範囲・ストローク・重力はすべて共通にし、**板の幅(= 安全に着地できる範囲)だけ**を変える。
難易度を切り替えても指の感覚が変わらないので3歳児が混乱せず、
かつ「やさしいほうが板が太い」と見た目で分かるのが利点。

| | やさしい | ふつう |
|---|---|---|
| 板の幅 | 広い(成功域 **60% 以上**) | 狭い(成功域 **30〜50%**) |
| 失敗のしかた | 強すぎ・弱すぎの両方 | 同じ |

- **調整の合格基準**: すべての遷移について、成功する引き量の範囲が
  やさしい 60%以上 / ふつう 30〜50%(ストローク比)であり、かつ**連続した1区間**であること。
  「ここは通るがもう少し引くと通らない、さらに引くとまた通る」という飛び地があってはならない。

---

## 5. ビジュアルデザイン指針

- **すべてCanvas 2Dのコード描画で完結させ、外部画像・音声・フォント素材は一切使わない。**
  権利フリー・ロード高速・オフラインでも壊れないため。
- 世界観: 絵本調。背景は空色のグラデーション、雲、太陽。盤面は実機の赤い筐体を模す。
- 色: 明るいパステル基調+はっきりした太い輪郭線(3歳児の視認性優先)。
  - 盤面: 黄緑 / 板: 木の板風の茶色 / レバー: 赤 / 穴: こげ茶の楕円 / 筐体: 赤+金の縁
- コイン: **どうぶつの顔が描かれた丸いメダル**(金色の縁+うさぎの顔)。半径28px。
- 各段の板の上に応援するどうぶつを1匹ずつ配置する。コインの通り道を避け、溝と反対側に立たせる。
- あたりの口: 旗が立った受け口。「あたり」と大きく書く。
- プランジャー: 実機風の赤い丸ノブ+バネの見た目。
- 未操作が2秒続いたら、指アイコンがノブを下へ引く**操作ガイド**をループ表示する。
  3歳児はひらがなを読めないため、これが唯一の操作説明になる。

## 6. スタンプ収集仕様

- あたり1回につきスタンプ1個獲得。
- スタンプは**どうぶつの顔スタンプ10種類**(うさぎ、くま、ぱんだ、りす、ねこ、いぬ、ぞう、きりん、ぺんぎん、らいおん)を順番に付与し、10個を超えたら2周目(同じ順)。
- 保存: `localStorage`。キー `flickshot.save.v1`、値はJSON:
  ```json
  { "stampCount": 12, "lastDifficulty": "easy" }
  ```
  - 獲得順は種類テーブルから `index % 10` で導出できるため、カウントのみ保存すれば十分。
  - localStorageが使えない環境(プライベートモード等)では例外を握りつぶし、
    セッション内メモリのみで動作させる(クラッシュさせない)。
- 表示: タイトル画面に総数+一覧、あたり時リザルトに「ぺたん!」押印アニメ。

---

## 7. 技術仕様

### 7.1 スタック

| 項目 | 選定 | 理由 |
|---|---|---|
| ビルド | **Vite**(最新安定版) | GitHub Pages向け静的ビルドが簡単 |
| 言語 | **TypeScript**(strict) | 引き継ぎ実装の安全性 |
| 描画 | **Canvas 2D API** | 依存ゼロ。パーティクルやアニメも十分賄える |
| 物理 | **自作簡易物理**(§4.2) | 優しい挙動の作り込みとチューニング容易性 |
| ランタイム依存 | **ゼロ**(devDependenciesのみ) | 軽量・保守容易 |
| テスト | **Vitest**(ユニットテストのみ) | 物理・判定ロジックの回帰防止 |
| 状態管理 | 自作シーンマシン | Title / Game / Result の3シーン切替のみで十分 |

### 7.2 ディレクトリ構成

```
/
├─ index.html
├─ vite.config.ts          # base: './'(相対パス。§7.5)
├─ package.json
├─ tsconfig.json
├─ plan.md                 # 本書
├─ docs/detailed-design.md # 詳細設計書
├─ scripts/verify-geometry.ts  # 盤面の検算(npm run verify)
├─ .github/workflows/deploy.yml
└─ src/
   ├─ main.ts              # 起動、Canvasセットアップ、シーンマシン、メインループ
   ├─ config.ts            # ★全チューニング定数(盤面座標、物理、難易度テーブル)
   ├─ scenes/
   │   ├─ title.ts
   │   ├─ game.ts
   │   └─ result.ts
   ├─ game/
   │   ├─ board.ts         # 段・穴・あたりの口の定義と当たり判定
   │   ├─ coin.ts          # コイン状態機械と物理更新
   │   ├─ plunger.ts       # プランジャー入力(Pointer Events)と引き量管理
   │   └─ levers.ts        # レバー群のアニメ
   ├─ render/
   │   ├─ layout.ts        # 720×1280論理座標→実画面フィット、DPR対応
   │   ├─ drawings.ts      # どうぶつ・盤面・メダル等のコード描画関数集
   │   ├─ animals.ts       # どうぶつの顔
   │   ├─ shapes.ts        # 図形・文字の下請け
   │   └─ particles.ts     # 紙吹雪等
   └─ save.ts              # localStorageラッパー(スタンプ)
```

### 7.3 入力まわりの必須実装

- Pointer Events を使用(Touch/Mouseを個別実装しない)。
- Canvas要素に `touch-action: none`(ドラッグ中のスクロール・ズーム発動を防ぐ)。
- `<meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">`。
- iOS Safariのダブルタップズーム・長押し選択の抑止(`user-select: none` 等)。
- `pointercancel` でも**発射扱いにする**。iOSでシステムジェスチャに割り込まれたときに
  操作が無かったことにならないようにするため。
- 「あきらめる」長押しは、押下開始からの経過で進捗リングを描画し、1秒到達で発動。
  途中で指が離れる/リング外に出たらキャンセル。

### 7.4 対応環境・非機能要件

- 対応ブラウザ: iOS Safari / Android Chrome / デスクトップChrome・Edge・Firefox・Safari の各最新版。
- 60fps目標(固定タイムステップ60Hz+rAF描画)。
- 通信: 初回ロード以外ネットワーク不要。外部リソース読み込みゼロ。
- アクセシビリティ: 文字サイズ大きめ、色のみに依存した情報伝達をしない(穴は形でも分かる)。
- PWA化・音・多言語・ランキングは**スコープ外**(実装しない)。

### 7.5 GitHub Pages デプロイ(GitHub Actions)

- リポジトリ: `sakanayuki/cc_flickshot` → 公開URLは `https://sakanayuki.github.io/cc_flickshot/`。
- `vite.config.ts` の `base` は **`'./'`(相対パス)**にする。
  絶対パス(`/cc_flickshot/`)にすると、配信パスが想定と1文字でも違ったときに
  JSが404になり、**画面が真っ青になったまま何のエラーも出ない**状態になる(§11 参照)。
- 起動に失敗したときは必ず画面に理由を出すこと(`showFatal`)。描画ループも try/catch で囲む。
- `.github/workflows/deploy.yml`:
  - トリガー: `push` to `main`(+ `workflow_dispatch`)。
  - ジョブ1 build: `actions/checkout` → Nodeセットアップ(LTS) → `npm ci`
    → `npm run verify` → `npm test` → `npm run build`
    → `actions/configure-pages` → `actions/upload-pages-artifact`(`dist/`)。
  - ジョブ2 deploy: `actions/deploy-pages`。`permissions: pages: write, id-token: write`。
- **手動設定が1つ必要**: リポジトリの Settings → Pages → Source を「**GitHub Actions**」にする。

### 7.6 テスト方針

Vitestで最低限以下をユニットテストする(描画はテスト不要):

- 盤面の幾何: 段の座標が定数から正しく導出され、5つの遷移が同一条件になっている。
- 弾き: 溝の近くにいるコインだけが弾かれる。向きが段ごとに交互になっている。
- 成功域: 各遷移について、成功する引き量の割合・連続性・**弱すぎと強すぎの両方の失敗が存在すること**。
- 投入直後の安全性: 一度も弾く前に落ちない。
- 頑健性: どの引き量でもコインが盤面外に出ない、状態が壊れない。
- save.ts: 保存・読込・localStorage例外時のフォールバック。

加えて `npm run verify` で盤面全体の検算を行う。**このスクリプトは放物線を解かない。
実際の物理を回して測る。** 計算上は正しいのに遊べない不具合を何度も作り込んだため(§11)。

---

## 8. 実装ステップ(推奨順序)

1. **足場**: Vite+TS雛形、レイアウト(720×1280フィット+DPR)、シーンマシン、空の3シーン遷移。
2. **盤面とコイン**: config.tsに段の座標定義 → コイン物理(onPlank/airborne)→ 板着地・穴落下判定。
3. **検算**: `npm run verify` で成功域を測り、板の幅と `P_MIN`/`P_MAX` を詰める。
4. **プランジャーとレバー**: ドラッグ入力、pull→power、全レバー連動。
5. **描画**: 段・穴・あたりの口・右上投入口・筐体・どうぶつ。
6. **ゲームフロー**: 投入アニメ → プレイ → 3種の終了 → リザルト、あきらめる長押し。
7. **スタンプ**: save.ts、獲得演出、タイトルのスタンプ帳。
8. **通しプレイ**: 実機サイズ(縦持ちスマホ)で必ず確認する。
9. **デプロイ**: workflow作成、Pages公開、README整備。

## 9. 受け入れチェックリスト

- [x] スマホ縦持ちで、盤面全体とプランジャーが1画面に収まり操作できる
- [x] 盤面が画面の 74%、プランジャー帯が 22% に収まっている
- [x] プランジャーは指の移動量240pxをドラッグでき、引き量に応じて弾く力が変わる
- [x] 発射で5段すべてのレバーが同時に動く
- [x] コインは**右上**から投入され、上から下へ降りていく
- [x] **弱すぎても強すぎても穴に落ちて没収される**(両方向に失敗がある)
- [x] あたりの口に入れたときだけクリアになる
- [x] メダル投入アニメ→プレイ→(穴/あたり/あきらめ)→リザルトの遷移がすべて機能する
- [x] あきらめるは1秒長押しでのみ発動し、誤タップでは何も起きない
- [x] 「やさしい」の成功域が全遷移で60%以上、「ふつう」が30〜50%
- [x] あたりでスタンプが増え、リロード後もタイトル画面に残っている
- [x] 音は一切鳴らない
- [x] 外部素材(画像・音・フォント・CDN)への参照が一切ない
- [ ] `https://sakanayuki.github.io/cc_flickshot/` で動作する(mainへのpushで自動デプロイ)

## 10. 未決事項(実装者の裁量に委ねる範囲)

- どうぶつの具体的なデザイン(§5の指針の範囲内で自由)。
- 投入アニメ・紙吹雪などの細かい演出時間(指定値は目安。±50%は裁量)。
- config.ts内の物理定数の初期値(§4.5の合格基準を満たすよう調整する前提)。
- これら以外の仕様変更(画面追加、操作方式変更、音の追加など)は**発注者確認なしに行わないこと**。

---

## 11. 改訂履歴

### 2026-07-28(3) — ルールの全面見直し「やまのぼり」→「やまくだり」

発注者より、**ゲームそのものを取り違えていた**との指摘があった。

> 根本的にこのゲームそのものを間違えて認識していました。これは上から一番下まで降りるゲームです。
> 適度な強さでコインを弾くと、溝から下の段に進むことができます。強く叩きすぎたり、弱く叩きすぎる
> 場合は穴に落ちてしまい、コインを没収される仕組みです。
> また、バネを画面の35%高さを占めていますが、もっとゲーム画面を大きく表示したいので、割合を小さくして。
> また、コインは右上に入れるものとし、コインは右上から下に進むものとして作り直して。

これまでの実装は「下から上へ登り、強すぎても失敗しない」という**真逆**の設計だった。
物理の中核(弾く方向・成功条件・失敗条件)がすべて反転するため、`game/` と盤面描画を作り直した。

#### 発注者と確定した仕様

| 論点 | 決定 |
|---|---|
| クリア条件 | 最下段の先に**「あたりの口」**があり、そこに入れて初めてクリア。外すと没収 |
| バネの帯 | 画面の **22%**(盤面は 74% に拡大)。指のドラッグ距離は 450px → **240px** |
| やさしいの救済 | **両難易度とも強すぎ・弱すぎの両方で落ちる**が、やさしいは成功域をストロークの **60% 以上**に取る |
| 段数 | **5段のまま**(+ 最後にあたりの口) |

#### 廃止した仕組み

旧ルール固有の以下は全廃した。新ルールでは意味を持たない。

- 上向きの発射シャフト、レーン先端・裏面の当たり判定、リップ越えの転落
- 転がり速度で穴に落ちる判定(`fallSpeed` / `holeRadius` / `lipEscapeSpeed`)
- 壁での跳ね返り(§4.2 のとおり、跳ね返らせるとルールが崩れる)
- ゴールバスケット(→ あたりの口)

#### 新ルールでのチューニング結果

| | やさしい | ふつう |
|---|---|---|
| 板の幅 | 330px | 160px |
| 各遷移の成功域 | 85% / 85% / 85% / 85% / 89% | 43% / 43% / 43% / 43% / 47% |
| 成功域の連続性 | すべて1区間 | すべて1区間 |
| 弱すぎ・強すぎの失敗 | 両方あり | 両方あり |

- `P_MAX` を 1400 にすると、やさしいでもストローク上端が丸ごと「強すぎ」になって成功域が
  52% に落ちた。1000 に下げて 85% を得た。
- 板幅を 340 にすると成功域は 87% になるが、**「強すぎ」の失敗が消滅**した
  (板が壁に接してしまい、強く弾いても壁で止まって板に戻る)。
  これは発注者の「両方で落ちる」という指定に反するため、330 に戻した。
  この違反は検算スクリプトの「弱すぎでも強すぎでも落ちる」チェックが自動で検出した。

### 2026-07-28(2) — 公開ページが真っ青になる不具合

発注者より「`https://sakanayuki.github.io/cc_flickshot/` にアクセスしましたが、
真っ青で何も表示されません」との報告。

原因は `vite.config.ts` の `base: '/cc_flickshot/'`(絶対パス)。配信パスが想定と違うと
JSが404になり、Canvasが初期サイズ(300×150)のまま残って body の背景色だけが見える。
**エラーも何も表示されないため、端末側からは原因が全く分からない。**

対処(いずれも再発防止として恒久化):

- `base: './'` に変更。どこから配信されても解決できるようにした
- 起動に失敗したら画面に理由を出す `showFatal()` を追加
- 描画ループを try/catch で囲み、描画で落ちたときも理由を出す
- `<noscript>` を追加
- `modulePreload: false`(バンドルから唯一の `fetch()` も消えた)
- `.nojekyll` を追加

### 2026-07-28(1) — 詳細設計書の作成にともなう追加確定

詳細設計書 [`docs/detailed-design.md`](docs/detailed-design.md) を作成し、
操作ガイド(未操作2秒ごとに毎回表示)などを追加確定した。

このとき「座標の計算は正しいのに遊べない」不具合を繰り返し作り込んだため、
検算スクリプトを**放物線を解く方式から、実際の物理を回して測る方式に変更**した。
この方針は新ルールでもそのまま引き継いでいる。
