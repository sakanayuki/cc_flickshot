# plan.md — コインフリックゲーム「どうぶつの やまのぼり」要件定義書

このドキュメントは発注者との対話で確定した**要件**(何を作るか)を記載している。

> **⚠️ 実装者へ: 実装時は [`docs/detailed-design.md`](docs/detailed-design.md)(詳細設計書)を参照すること。**
> 座標・物理定数・型定義・関数シグネチャはすべてそちらに確定値で書かれている。
> **具体的な数値が本書と詳細設計書で食い違う場合、詳細設計書を正とする。**
> 本書の数値は要件検討時の目安であり、詳細設計書で物理的な検算を行った結果、一部を変更している(§11)。
>
> 本書は「なぜそうなっているか」という背景と意図を理解したいときに読む。

---

## 1. コンセプト

- 昔の駄菓子屋にあった「10円ゲーム機」(新幹線ゲーム系のエレメカ筐体)のWebアプリ版。
  実機では、投入したコインがジグザグの坂を転がり、プレイヤーが複数のレバーでコインを
  弾き上げて頂上のゴールを目指す。途中の穴に落ちたら失敗。
- **ターゲットは3歳児**。保護者と一緒に、または一人で遊べること。
  すべてのUI・難易度・演出は「3歳児が理解でき、失敗しても泣かない」ことを最優先に設計する。
- テーマは「**どうぶつの山のぼり**」。どうぶつメダルを弾いて山頂を目指す絵本調の世界観。

### 実機との最重要な差分(発注者指定)

実機ではレバーは1本ずつ独立操作だが、本作では
**画面右下の「引っ張れるプランジャーボタン」1つで全レバーが連動して同時に動く**。

- 引っ張った量が大きいほど、弾く力が強くなる。
- シビアな操作を減らすため、**引っ張れるストロークは長めに確保する**(後述: 画面高さの約35%)。

---

## 2. 確定済み要件(発注者との合意事項)

| 論点 | 決定内容 |
|---|---|
| 操作方式 | 右下プランジャー1つで全レバー連動。引き量=弾く力。ストローク長め |
| 画面構成 | タイトル画面 / ゲーム画面 / リザルト画面 の3画面 |
| ゲーム開始 | メダル投入アニメーションから始まる |
| リザルト遷移条件 | ①穴に落ちる ②ゴールに入る ③「あきらめる」長押し の3つのみ |
| デバイス | **スマホ/タブレット縦向き優先**(PCでは中央に縦長キャンバス表示) |
| テーマ | **どうぶつの山のぼり**(絵本調、パステル+はっきりした輪郭) |
| サウンド | **音なし**(BGM・効果音ともに実装しない。ミュートボタンも不要) |
| 難易度 | **2段階「やさしい / ふつう」**をタイトル画面で選択 |
| ごほうび | **スタンプ収集あり**。ゴールごとにどうぶつスタンプ1個獲得、localStorageに永続化 |
| あきらめるボタン | **左上に小さく配置、1秒長押しで発動**(誤タップ対策。進捗リングを表示) |
| デプロイ | GitHub Actions で GitHub Pages に自動デプロイ |

---

## 3. 画面仕様

### 3.1 共通レイアウト

- 論理座標系: **720 × 1280(9:16 縦)**。全シーンこの座標系で描画する。
- 実画面へは アスペクト比維持でフィット(レターボックス)。余白は背景色(空色)で塗る。
- `devicePixelRatio` を考慮して Canvas の実ピクセルサイズを設定し、にじみを防ぐ。
- 文字は**ひらがな中心・最小限**。可能な限りアイコンと絵で伝える。
- フォントはシステムフォント(`sans-serif` 系)でよい。外部フォント読み込みはしない。

### 3.2 タイトル画面

- 内容:
  - タイトルロゴ「どうぶつの やまのぼり」(Canvas描画。山+旗+どうぶつの飾り)。
  - 難易度ボタン2つ(大きく、縦に並べる):
    - 「やさしい」(推奨。デフォルトで強調表示)
    - 「ふつう」
  - どちらかをタップすると即ゲーム画面へ(確認ダイアログなどは挟まない)。
  - 画面下部に**スタンプ帳エリア**: 集めたスタンプ数(例: 「スタンプ 7こ」)と、
    獲得済みスタンプを小さく並べて表示。0個のときは空の枠を数個見せて収集意欲を誘う。
- 3歳児配慮: ボタンは指が多少ズレても押せるサイズ(論理座標で高さ120px以上)。

### 3.3 ゲーム画面

レイアウト(縦720×1280内):

```
+----------------------------------+
| [あきらめる(小)]                  |  ← 左上。小さく。1秒長押しで発動
|                                  |
|   ┌─ 盤面(山) ─────────┐          |
|   │  ゴール(山頂の旗)   │          |
|   │  ~ジグザグ坂×5段~   │          |  ← 画面上部 約70%
|   │  穴 / レバー / 応援動物│        |
|   └────────────────────┘          |
|                                  |
|  メダル投入口(左下)                |
|                    [プランジャー] |  ← 右下。ストローク長 約450px
+----------------------------------+
```

- 開始時: **メダル投入アニメーション**(約1.5秒)。
  どうぶつメダルが投入口にコトンと入り、盤面最下段に転がり出てくる。
  この間プランジャーは操作不能(見た目はグレーではなく通常のまま、引いても反応しないだけ)。
- プレイ中: プランジャー操作でメダルを弾き上げ、山頂ゴールを目指す。
- 終了条件(いずれかで即リザルト画面へ遷移。遷移前に短い演出を挟む):
  1. メダルが穴に落ちる(落下アニメ約1秒 → リザルト)
  2. メダルがゴールポケットに入る(山頂で旗が揺れ、どうぶつがジャンプ 約1.5秒 → リザルト)
  3. あきらめるボタン1秒長押し完了(即リザルト)

### 3.4 リザルト画面

結果に応じて3パターンの表示。いずれも**責める演出は絶対にしない**。

| 結果 | 見出し(ひらがな) | 演出 |
|---|---|---|
| ゴール | 「やったね!」 | 紙吹雪(Canvasパーティクル)、どうぶつが山頂でバンザイ、**スタンプ獲得演出**(スタンプが「ぺたん!」と画面に押されるアニメ) |
| 穴落ち | 「おしい!」 | どうぶつが穴からひょっこり顔を出して笑う。悲しい表現は使わない |
| あきらめ | 「またね!」 | どうぶつが手を振る |

- 共通表示: 今回の**到達段数**を山の絵で表示(例: 5段中3段目まで登った旗マーク)。
- ボタン:
  - 「もういちど」(大): 同じ難易度で即ゲーム画面へ(メダル投入アニメから)。
  - 「さいしょから」(小): タイトル画面へ。

---

## 4. ゲームデザイン詳細

### 4.1 盤面構成

- 山をモチーフにした盤面に、**左右ジグザグの坂レーンを5段**配置する(下から段1〜段5)。
- 各レーンは緩い坂(水平から約8°)。コインは重力で低い方へ転がる。
- レーンの**低い側の端に「弾きゾーン+レバー」**がある。
  - 段1のレバーは右端、段2は左端、段3は右端…と交互(ジグザグ)。
  - コインは坂を転がってレバー側の端に自然に到達し、壁に当たって止まる。
- 段5(最上段)の端には**ゴールポケット**(山頂の旗が立った受け口)。
  段5では弾き上げではなく、適正な力で弾くとゴールポケットに飛び込む。
- 各レーンの中間に**穴**(次節)。
- メダル投入口は画面左下。投入アニメ後、メダルは段1の高い側から転がり出る。

### 4.2 コインの挙動(自作簡易物理)

物理エンジン(Matter.js等)は**使わない**。理由: 3歳児向けに「理不尽な跳ね方をしない、
決定論的で優しい挙動」を作り込む必要があり、自作の単純モデルの方が調整しやすいため。

コインは常に次の3状態のいずれか:

1. **onLane(レーン上)**: レーン線分に沿った1次元運動。
   - 加速度 = 重力の坂方向成分(g·sinθ) − 転がり摩擦(速度に比例)。
   - レバー側端の壁に当たったら速度0で停止。
2. **airborne(空中)**: 弾かれた後の放物運動(重力のみ。空気抵抗なし)。
   - いずれかのレーン線分と上から交差したら着地 → onLane へ。着地時、
     法線方向速度は捨て、接線方向速度のみ残す(バウンドさせない。挙動を読みやすくするため)。
   - 盤面側壁に当たったら水平速度を反転×0.5(軽く跳ね返る)。
   - 盤面天井に当たったら垂直速度を0にして落下開始。
3. **falling(穴落下)**: 穴に吸い込まれる演出状態。操作不能。縮小しながら穴中心へ。約1秒でリザルトへ。

- 更新は**固定タイムステップ(60Hz、accumulator方式)**。描画はrequestAnimationFrame。
- 座標・速度などのチューニング値はすべて1つの定数ファイル(`config.ts`)に集約すること(§7)。

### 4.3 穴の判定(難易度の核)

- 穴はレーン上の円形領域。
- onLane状態のコインが穴の中心±穴半径内にいるとき、
  **|転がり速度| < 落下閾値 なら落ちる。閾値以上なら勢いで飛び越えて通過**する。
- これにより自然なゲーム性が生まれる:
  - 適正な力で弾かれたコインは着地後も勢いがあり、穴を飛び越えてレバー側に到達する。
  - 弱すぎる弾きで届かず同じ段に戻ってきたコインは、減速していて穴に落ちやすい。

### 4.4 プランジャー(唯一の操作)仕様

- 位置: 画面右下。つまみサイズは論理座標で **幅140×高さ140px 以上**(3歳の指でも掴みやすく)。
- 操作: Pointer Events(`pointerdown/move/up/cancel`)でタッチ・マウス両対応。
  - つまみを押さえて**下方向にドラッグ**するとつまみが下がる。
  - **ストローク長: 450px(論理座標。画面高さの約35%)** — 発注者指定の「長め」要件。
  - 指を離した瞬間(`pointerup` / `pointercancel` どちらでも)に**発射**。
  - 引き量 `pull ∈ [0, 1]` を弾き力にマップ: `power = P_MIN + (P_MAX − P_MIN) × pull`。
  - 引き量が極小(pull < 0.05)のときは発射せず、つまみが戻るだけ(誤タップで空撃ちさせない)。
- つまみはバネ表現で戻る(離した後0.15秒程度で上端へ)。引いている間、盤面の**全レバーが
  連動してわずかに下がる**(タメの視覚フィードバック)。
- 発射時: **全レバーが同時に**はたき上げアニメーション(約0.2秒)。
- レバーの当たり判定: 各レバー先端に**弾きゾーン**(コイン直径の約3倍幅の矩形)を持つ。
  発射時にゾーン内にいるコインへ、上方向+盤面内側方向の初速を与える。
  - 初速ベクトル = `power` を大きさとし、角度は鉛直から内側へ約20°。
  - **弾きゾーンを広くすることが「シビアな操作を減らす」要件の実装手段である。狭くしないこと。**
- 連打制限: レバー作動後0.3秒はプランジャー再発射不可(見た目のクールダウン。表示は不要)。

### 4.5 難易度パラメータ(2段階)

| パラメータ | やさしい | ふつう |
|---|---|---|
| 穴の数 | 各段1個 | 段1〜2は1個、段3〜5は2個 |
| 穴の半径 | コイン半径 × 0.9(小さめ) | コイン半径 × 1.2 |
| 落下閾値(これ未満の速度で落ちる) | 低め(かなり減速していないと落ちない) | 中程度 |
| 弾き力の適正範囲 | 広い(pull 0.3〜0.9でおおむね次段に届く) | 中(pull 0.45〜0.8) |
| 期待される結果 | 3歳児でも高確率でゴールできる | 大人でも数回に1回ゴールできる程度 |

- 具体的な数値は `config.ts` に難易度別テーブルとして定義し、実装後に実プレイで調整する。
- **調整の合格基準**: 「やさしい」で、pull量をランダムにしても5回中4回以上ゴールできること。
  「ふつう」で、狙って引けば大人が3回に1回程度ゴールできること。

### 4.6 弾き力と結果の対応(設計意図)

3歳児向けに「強すぎても大失敗しない」ことが重要:

- **弱すぎる**: 次の段に届かず、同じ段に落ちて戻る(減速して穴リスクは上がるが、即死ではない)。
- **適正**: 次の段に着地し、勢いで穴を飛び越えてレバー側端へ。
- **強すぎる**: 天井や側壁に当たって減速し、上の段か同じ段に落ちる。
  **「強すぎたせいで穴に直撃して即死」という配置にはしないこと**(穴はレバー着地点の直下に置かない)。

---

## 5. ビジュアルデザイン指針

- **すべてCanvas 2Dのコード描画(またはインラインSVG→描画)で完結させ、外部画像・音声・フォント素材は一切使わない。**
  権利フリー・ロード高速・オフラインでも壊れないため。
- 世界観: 絵本調の山。背景は空色→山頂に向かうグラデーション。雲、太陽。
- 色: 明るいパステル基調+はっきりした太い輪郭線(3歳児の視認性優先)。
  - 山肌: 黄緑〜緑 / レーン: 木の板風の茶色 / レバー: 赤 / 穴: こげ茶の楕円
- コイン: **どうぶつの顔が描かれた丸いメダル**(金色の縁+くまの顔など)。直径は論理座標で約56px(=半径28)。
- 各レーンの脇に応援するどうぶつ(うさぎ、りす、ぱんだ等)を静的に配置。単純な図形の組み合わせで描く。
- ゴール: 山頂に旗と「ゴールポケット」。ゴール時は旗が揺れる。
- 穴: モグラがときどき顔を出す(ランダムな待機アニメ。ゲーム性には影響しない、見た目だけ)。
- プランジャー: 実機風の赤い丸ノブ+バネの見た目。「ひっぱってね」の矢印を初回だけ点滅表示。

## 6. スタンプ収集仕様

- ゴール1回につきスタンプ1個獲得。
- スタンプは**どうぶつの顔スタンプ10種類**(うさぎ、くま、ぱんだ、りす、ねこ、いぬ、ぞう、きりん、ぺんぎん、らいおん)を順番に付与し、10個を超えたら2周目(同じ順)。
- 保存: `localStorage`。キー `flickshot.save.v1`、値はJSON:
  ```json
  { "stampCount": 12 }
  ```
  - 獲得順は種類テーブルから `index % 10` で導出できるため、カウントのみ保存すれば十分。
  - localStorageが使えない環境(プライベートモード等)では例外を握りつぶし、セッション内メモリのみで動作させる(クラッシュさせない)。
- 表示: タイトル画面に総数+一覧、ゴール時リザルトに「ぺたん!」押印アニメ。

---

## 7. 技術仕様

### 7.1 スタック

| 項目 | 選定 | 理由 |
|---|---|---|
| ビルド | **Vite**(最新安定版) | GitHub Pages向け静的ビルドが簡単。`base`設定でサブパス対応 |
| 言語 | **TypeScript**(strict) | 引き継ぎ実装の安全性 |
| 描画 | **Canvas 2D API** | 依存ゼロ。パーティクルやアニメも十分賄える |
| 物理 | **自作簡易物理**(§4.2) | 優しい挙動の作り込みとチューニング容易性 |
| ランタイム依存 | **ゼロ**(devDependenciesのみ) | 軽量・保守容易 |
| テスト | **Vitest**(ユニットテストのみ) | 物理・判定ロジックの回帰防止 |
| 状態管理 | 自作シーンマシン | Title / Game / Result の3シーン切替のみで十分 |

### 7.2 ディレクトリ構成(想定)

```
/
├─ index.html
├─ vite.config.ts          # base: '/cc_flickshot/'
├─ package.json
├─ tsconfig.json
├─ plan.md                 # 本書
├─ .github/workflows/deploy.yml
└─ src/
   ├─ main.ts              # 起動、Canvasセットアップ、シーンマシン、メインループ
   ├─ config.ts            # ★全チューニング定数(レーン座標、物理、難易度テーブル)
   ├─ scenes/
   │   ├─ title.ts
   │   ├─ game.ts
   │   └─ result.ts
   ├─ game/
   │   ├─ board.ts         # レーン・穴・ゴール・壁の定義と当たり判定
   │   ├─ coin.ts          # コイン状態機械(onLane/airborne/falling)と物理更新
   │   ├─ plunger.ts       # プランジャー入力(Pointer Events)と引き量管理
   │   └─ levers.ts        # レバー群のアニメと弾きゾーン判定
   ├─ render/
   │   ├─ layout.ts        # 720×1280論理座標→実画面フィット、DPR対応
   │   ├─ drawings.ts      # どうぶつ・山・メダル等のコード描画関数集
   │   └─ particles.ts     # 紙吹雪等
   └─ save.ts              # localStorageラッパー(スタンプ)
```

### 7.3 入力まわりの必須実装

- Pointer Events を使用(Touch/Mouseを個別実装しない)。
- Canvas要素に `touch-action: none`(ドラッグ中のスクロール・ズーム発動を防ぐ)。
- `<meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">`。
- iOS Safariのダブルタップズーム・長押し選択の抑止(`user-select: none` 等)。
- 「あきらめる」長押しは、押下開始からの経過で進捗リングを描画し、1秒到達で発動。
  途中で指が離れる/リング外に出たらキャンセル。

### 7.4 対応環境・非機能要件

- 対応ブラウザ: iOS Safari / Android Chrome / デスクトップChrome・Edge・Firefox・Safari の各最新版。
- 60fps目標(固定タイムステップ60Hz+rAF描画)。
- 通信: 初回ロード以外ネットワーク不要。外部リソース読み込みゼロ。
- アクセシビリティ: 文字サイズ大きめ、色のみに依存した情報伝達をしない(穴は形でも分かる)。
- PWA化・音・多言語・ランキングは**スコープ外**(実装しない)。

### 7.5 GitHub Pages デプロイ(GitHub Actions)

- リポジトリ: `sakanayuki/cc_flickshot` → 公開URLは `https://sakanayuki.github.io/cc_flickshot/`。
- `vite.config.ts` に `base: '/cc_flickshot/'` を必ず設定(忘れるとアセット404になる)。
- `.github/workflows/deploy.yml`:
  - トリガー: `push` to `main`(+ `workflow_dispatch`)。
  - ジョブ1 build: `actions/checkout` → Nodeセットアップ(LTS) → `npm ci` → `npm run build`
    → `actions/configure-pages` → `actions/upload-pages-artifact`(`dist/`)。
  - ジョブ2 deploy: `actions/deploy-pages`。`permissions: pages: write, id-token: write`。
    `environment: github-pages`。
- **手動設定が1つ必要**: リポジトリの Settings → Pages → Source を「**GitHub Actions**」にする。
  実装完了時、この手順をREADMEに記載し、発注者に伝えること。

### 7.6 テスト方針

Vitestで最低限以下をユニットテストする(描画はテスト不要):

- 穴判定: 閾値未満の速度で落ちる/以上で通過する。
- 弾きゾーン判定: ゾーン内のコインのみ初速が付与される。
- pull→power のマッピング(境界値: 0, 0.05未満は不発, 1)。
- 放物運動のレーン着地判定。
- save.ts: 保存・読込・localStorage例外時のフォールバック。

---

## 8. 実装ステップ(推奨順序)

1. **足場**: Vite+TS雛形、レイアウト(720×1280フィット+DPR)、シーンマシン、空の3シーン遷移。
2. **盤面とコイン**: config.tsにレーン座標定義 → 描画 → コイン物理(onLane/airborne)→ 穴・ゴール判定。
   ※この時点ではデバッグ用にクリックで固定力発射でよい。
3. **プランジャーとレバー**: ドラッグ入力、pull→power、全レバー連動、弾きゾーン。
4. **ゲームフロー**: 投入アニメ → プレイ → 3種の終了 → リザルト、あきらめる長押し。
5. **難易度2段階**: 難易度テーブル適用、タイトルで選択。
6. **ビジュアル仕上げ**: どうぶつ・山・演出(紙吹雪、モグラ、旗)。
7. **スタンプ**: save.ts、獲得演出、タイトルのスタンプ帳。
8. **チューニング**: §4.5の合格基準を満たすまで config.ts を調整。実機(スマホ)で必ず確認。
9. **デプロイ**: workflow作成、Pages公開、README整備。

## 9. 受け入れチェックリスト

- [ ] スマホ縦持ちで、盤面全体とプランジャーが1画面に収まり操作できる
- [ ] プランジャーはストローク450px(論理)をドラッグでき、引き量に応じて弾く力が変わる
- [ ] 発射で5段すべてのレバーが同時に動く
- [ ] メダル投入アニメ→プレイ→(穴/ゴール/あきらめ)→リザルトの遷移がすべて機能する
- [ ] あきらめるは1秒長押しでのみ発動し、誤タップでは何も起きない
- [ ] 「やさしい」は適当に引いてもおおむねゴールできる(5回中4回以上)
- [ ] ゴールでスタンプが増え、リロード後もタイトル画面に残っている
- [ ] 音は一切鳴らない
- [ ] `https://sakanayuki.github.io/cc_flickshot/` で動作する(mainへのpushで自動デプロイ)
- [ ] 外部素材(画像・音・フォント・CDN)への参照が一切ない

## 10. 未決事項(実装者の裁量に委ねる範囲)

- どうぶつの具体的なデザイン(§5の指針の範囲内で自由)。
- 投入アニメ・紙吹雪などの細かい演出時間(指定値は目安。±50%は裁量)。
- config.ts内の物理定数の初期値(§4.5の合格基準を満たすよう調整する前提)。
- これら以外の仕様変更(画面追加、操作方式変更、音の追加など)は**発注者確認なしに行わないこと**。

---

## 11. 改訂履歴

### 2026-07-28 — 詳細設計書の作成にともなう追加確定・変更

詳細設計書 [`docs/detailed-design.md`](docs/detailed-design.md) を作成した。
その過程で発注者に確認して追加確定した事項と、物理的な検算の結果として本書から変更した数値を記録する。

#### 追加確定した仕様(発注者回答)

| 論点 | 決定 | 詳細設計書 |
|---|---|---|
| 下段への転落 | **難易度で分ける**。やさしい=レーン端のリップが高く転落なし / ふつう=リップが低く、速く転がりすぎると1段下へ落ちる | §5.4 |
| ゴール判定 | **両難易度とも外れることがある**。ただし外れたコインは段5に戻るだけで、即失敗にはしない | §5.3 |
| 操作ガイド | **未操作が2秒続くたびに毎回表示**。指アイコンがノブを引く動きをループし、触れたら消える。3歳児はひらがなを読めないため、これが唯一の操作説明になる | §8.5 |

#### 検算の結果、本書から変更した数値

| # | 本書の記述 | 変更後 | 理由 |
|---|---|---|---|
| 1 | 盤面は画面上部**約70%**(§3.3) | 盤面は y=40..750(710px、約55%) | 盤面70%だと下部に384pxしか残らず、ストローク450pxのドラッグができないため |
| 2 | プランジャーのストローク**450px**(§4.4) | **指の移動量450px / ノブの見た目の移動量380px** に分離 | 発注者要件である「指450px」を維持したまま、ノブを画面内に収めるため |
| 3 | レーンの傾き **約8°**(§4.1) | **5.10°**(レーン長450pxに対し落差40px) | 8°ではレーンの落差が81pxとなり段間距離105pxを超え、跳躍量が段ごとに破綻するため |
| 4 | 難易度で**弾き力の適正範囲**を変える(§4.5) | 弾き力の範囲は**両難易度共通**。難易度差は穴の数・穴の半径・落下閾値・ゴール口の幅・リップの高さの5つだけで作る | 難易度を切り替えたときに指の感覚が変わると3歳児が混乱するため |
| 5 | 「ひっぱってね」矢印を**初回だけ**表示(§5) | **未操作2秒ごとに毎回**表示(上記の追加確定事項) | 3歳児はひらがなを読めないため |
| 6 | 弾きゾーンは**コイン直径の約3倍**(=168px、§4.4) | **135px**(約2.4倍) | 168pxだとゾーンが「上の段の真下」まで及んでしまい、そこから弾いてもコインは上の段の裏面に当たるだけで前に進めないため。なおコインは必ずレバー端の壁で停止するので、弾く瞬間のコイン位置は常に同じであり、ゾーン幅は体感にほぼ影響しない。「シビアな操作を減らす」要件はストローク450px・横ブレの無視・成功域74%で実現している |

#### 実装で判明した変更(2026-07-28 追記)

実装して実際に動かした結果、設計時の計算では見えなかった問題が5つ見つかった。
いずれも「座標の計算は正しいのに遊べない」種類のもので、詳細と対処は
[`docs/detailed-design.md` §0.3.1](docs/detailed-design.md) に記録してある。要点のみ:

- **弾き角を 20° → 26° に変更。** シャフトの上は2段上のレーンの裏面で塞がっており、
  20° ではコインが次の段の先端を越える前に天井に当たって戻されるため、成功域が
  ストロークの 2% しかなかった。26° で 73% になる
- **投入されたコインに初速を与えるようにした。** ふつうで、一度も弾く前に段1の穴へ
  落ちる状態だった
- **ふつうの転落閾値を上げた。** ほどほどの強さで弾くたびに毎回1段下へ落ちていた
- レーン先端の当たり判定を追加(コインがレーンをすり抜けていた)
- 壁の位置をコイン半径ぶん外へ(静止したコインが筐体からはみ出して見えていた)

このため検算スクリプトは「放物線を解く」方式をやめ、**実際の物理を回して測る**方式に
変えてある。上記5つはすべて `npm run verify` と `npm test` が守っている。

#### 検算で裏付けが取れた点

- 盤面の座標を「5つの跳躍がすべて**横150px・上145px**という同一条件になる」ように逆算して確定した。これにより弾き力の適正範囲が段によってばらつかず、チューニング定数が2つ(`P_MIN`/`P_MAX`)で済む
- 必要初速は **904 px/s**。`P_MIN=620, P_MAX=1700` とすることで **pull ≥ 0.27 でどの段からでも次段に到達**する(ストロークの74%が成功域)。本書 §4.5 が想定していた「やさしい: pull 0.3〜0.9」とほぼ一致した
- 本書 §4.6 の「強すぎても大失敗しない」という要件は、「上の段の裏面に当たったら垂直速度を0にする」という物理ルールだけで自然に成立することが確認できた。特別な救済処理は不要
- ゴールの成功域は やさしい 74% / ふつう 51%(ストローク比)となり、「両方とも外れることがある」という発注者の指定を満たす
